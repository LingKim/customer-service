---
title: "12 实现邀请客服成员功能（二）"
source: "https://articles.zsxq.com/id_zwnbf85cpwgt.html"
author:
  - "[[苏三]]"
published:
created: 2026-09-13
description:
tags:
  - "clippings"
---
[来自： Java突击队&AI项目实战](https://wx.zsxq.com/group/28851182188851)

## 五、前端：新增/修改文件总览

### 5.1 新增文件

src/api/member.ts  
src/views/members/index.vue  
src/views/invite/accept.vue

### 5.2 修改文件

src/router/index.ts  
src/layouts/AdminLayout.vue  
src/components/OnboardingGuide.vue  
src/views/enterprise/index.vue

### 5.3 API 封装

### 文件：yunti-frontend/src/api/member.ts

``` code-block-container
import { request } from './request'

export interface MemberRoleOption {
  code: string
  name: string
}

export interface MemberItem {
  userId: number
  userNo: string
  name: string
  phone?: string
  email?: string
  avatar?: string
  roleCode: string
  roleName: string
  status: number
  lastLoginTime?: string
  joinTime?: string
}

export interface InviteItem {
  id: string
  inviteCode: string
  phone?: string
  email?: string
  roleCode?: string
  roleName?: string
  inviterName?: string
  usedByName?: string
  status: number
  statusText: string
  expireTime?: string
  createTime?: string
}

export interface CreateInviteResult {
  id: string
  inviteCode: string
  roleCode: string
  roleName: string
  expireTime: string
  phone?: string
  email?: string
  emailSent: boolean
}

export interface InvitePreview {
  inviteCode: string
  roleCode?: string
  roleName?: string
  inviterName?: string
  phoneMasked?: string
  emailMasked?: string
  expireTime?: string
}

export interface AcceptResult {
  userId: number
  userNo: string
  name: string
  phone?: string
  email?: string
  roleCode: string
  roleName: string
  tenantCode: string
}

export interface MemberAccess {
  canManage: boolean
  roleCode?: string
  roleName?: string
}

/** 当前企业用户是否可管理成员及角色信息 */
export function fetchMemberAccess(): Promise<MemberAccess> {
  return request<MemberAccess>({ url: '/user/members/access', method: 'get' })
}

/** 可邀请角色 */
export function listMemberRoles(): Promise<MemberRoleOption[]> {
  return request<MemberRoleOption[]>({ url: '/user/members/roles', method: 'get' })
}

/** 企业成员列表 */
export function listMembers(): Promise<MemberItem[]> {
  return request<MemberItem[]>({ url: '/user/members', method: 'get' })
}

/** 邀请记录 */
export function listMemberInvites(): Promise<InviteItem[]> {
  return request<InviteItem[]>({ url: '/user/members/invites', method: 'get' })
}

/** 创建邀请 */
export function createMemberInvite(data: {
  phone?: string
  email?: string
  roleCode: string
  expireDays?: number
}): Promise<CreateInviteResult> {
  return request<CreateInviteResult>({ url: '/user/members/invites', method: 'post', data })
}

/** 撤销邀请 */
export function revokeMemberInvite(inviteCode: string): Promise<void> {
  return request<void>({ url: `/user/members/invites/${inviteCode}/revoke`, method: 'post' })
}

/** 重发邀请邮件 */
export function resendMemberInvite(inviteCode: string): Promise<boolean> {
  return request<boolean>({ url: `/user/members/invites/${inviteCode}/resend`, method: 'post' })
}

/** 公共邀请预览 */
export function fetchInvitePreview(code: string): Promise<InvitePreview> {
  return request<InvitePreview>({ url: `/user/members/invites/${code}/preview`, method: 'get' })
}

/** 接受邀请 */
export function acceptMemberInvite(
  code: string,
  data: { name: string; password: string; phone?: string; email?: string },
): Promise<AcceptResult> {
  return request<AcceptResult>({ url: `/user/members/invites/${code}/accept`, method: 'post', data })
}
```

### 5.4 成员管理页

“系统设置”不再是企业端菜单。企业管理员进入“团队管理 → 成员管理”：

### 文件：yunti-frontend/src/views/members/index.vue

``` code-block-container
<template>
  <div class="settings-page">
    <div class="page-title-row">
      <div>
        <h2 class="page-title">成员管理</h2>
        <p class="page-sub">客服团队成员 · 邀请加入 · 角色与状态管理</p>
      </div>
      <el-button type="primary" @click="openInviteDialog">
        <el-icon class="btn-icon"><Plus /></el-icon>
        邀请成员
      </el-button>
    </div>
    <OnboardingGuide @refresh="loadAll" />

    <div class="stat-row">
      <div class="stat-card">
        <div class="stat-value">{{ members.length }}</div>
        <div class="stat-label">成员总数</div>
      </div>
      <div class="stat-card">
        <div class="stat-value">{{ invitePendingCount }}</div>
        <div class="stat-label">邀请中</div>
      </div>
      <div class="stat-card">
        <div class="stat-value">{{ invites.filter((i) => i.status === 2).length }}</div>
        <div class="stat-label">已加入</div>
      </div>
    </div>
    <el-card shadow="never" class="main-card">
      <el-tabs v-model="activeTab">
        <el-tab-pane label="成员列表" name="members">
          <el-table :data="members" v-loading="loadingMembers" stripe>
            <el-table-column type="index" label="序号" width="70" />
            <el-table-column label="成员" min-width="220">
              <template #default="{ row }">
                <div class="member-cell">
                  <div class="member-avatar">{{ row.name.slice(0, 1) }}</div>
                  <div>
                    <div class="member-name">
                      {{ row.name }}
                      <el-tag v-if="row.roleCode === 'ADMIN'" size="small" type="danger" effect="light">管理员</el-tag>
                    </div>
                    <div class="member-sub mono">{{ row.userNo }}</div>
                  </div>
                </div>
              </template>
            </el-table-column>
            <el-table-column label="角色" width="150">
              <template #default="{ row }">
                <el-tag :type="roleTagType(row.roleCode)" effect="light">{{ row.roleName }}</el-tag>
              </template>
            </el-table-column>
            <el-table-column label="联系方式" min-width="220">
              <template #default="{ row }">
                <div class="contact-line">{{ row.email || '—' }}</div>
                <div class="member-sub">{{ row.phone || '未绑定手机号' }}</div>
              </template>
            </el-table-column>
            <el-table-column label="状态" width="110">
              <template #default="{ row }">
                <el-tag :type="row.status === 1 ? 'success' : 'info'" effect="light">
                  {{ row.status === 1 ? '在职' : '停用' }}
                </el-tag>
              </template>
            </el-table-column>
            <el-table-column label="最近活跃" width="170">
              <template #default="{ row }">{{ formatTime(row.lastLoginTime) }}</template>
            </el-table-column>
            <el-table-column label="加入时间" width="170">
              <template #default="{ row }">{{ formatTime(row.joinTime) }}</template>
            </el-table-column>
          </el-table>
        </el-tab-pane>
        <el-tab-pane label="邀请记录" name="invites">
          <div class="invite-tip">
            <el-icon :size="16"><Bell /></el-icon>
            <span>邀请链接默认 7 天有效，对方打开链接完成注册后将自动加入本企业并绑定所选角色。</span>
          </div>
          <el-table :data="invites" v-loading="loadingInvites" stripe>
            <el-table-column type="index" label="序号" width="70" />
            <el-table-column label="邀请对象" min-width="200">
              <template #default="{ row }">
                <div>{{ row.email || row.phone || '—' }}</div>
                <div class="member-sub mono">{{ row.inviteCode }}</div>
              </template>
            </el-table-column>
            <el-table-column label="角色" width="130">
              <template #default="{ row }">{{ row.roleName || '—' }}</template>
            </el-table-column>
            <el-table-column label="邀请人" width="130">
              <template #default="{ row }">{{ row.inviterName || '—' }}</template>
            </el-table-column>
            <el-table-column label="状态" width="110">
              <template #default="{ row }">
                <el-tag :type="inviteTagType(row.status)" effect="light">{{ row.statusText }}</el-tag>
              </template>
            </el-table-column>
            <el-table-column label="创建时间" width="170">
              <template #default="{ row }">{{ formatTime(row.createTime) }}</template>
            </el-table-column>
            <el-table-column label="有效期至" width="170">
              <template #default="{ row }">{{ formatTime(row.expireTime) }}</template>
            </el-table-column>
            <el-table-column label="操作" width="280" fixed="right">
              <template #default="{ row }">
                <el-button v-if="row.status === 1" link type="primary" @click="copyInviteUrl(row.inviteCode)">
                  复制链接
                </el-button>
                <el-button
                  v-if="row.status === 1 && row.email"
                  link
                  type="primary"
                  :loading="resendingCode === row.inviteCode"
                  @click="resend(row)"
                >
                  重发邮件
                </el-button>
                <el-button v-if="row.status === 1" link type="danger" @click="revoke(row)">
                  撤销
                </el-button>
                <span v-else class="member-sub">—</span>
              </template>
            </el-table-column>
          </el-table>
        </el-tab-pane>
      </el-tabs>
    </el-card>
    <el-dialog
      v-model="inviteVisible"
      title="邀请客服成员"
      width="560px"
      :close-on-click-modal="false"
    >
      <el-form label-position="top">
        <div class="field-tip">填写对方邮箱后，系统会发送一封专业邀请邮件；对方点击邮件按钮即可进入邀请页完成注册。</div>
        <el-form-item label="受邀邮箱" required>
          <el-input v-model="inviteForm.email" maxlength="128" placeholder="例如：agent@example.com" clearable />
        </el-form-item>
        <el-form-item label="绑定手机号（可选）">
          <el-input v-model="inviteForm.phone" maxlength="20" placeholder="用于登录与身份校验" clearable />
        </el-form-item>
        <el-form-item label="角色">
          <el-select v-model="inviteForm.roleCode" class="full-width" placeholder="请选择成员角色">
            <el-option v-for="role in roleOptions" :key="role.code" :label="role.name" :value="role.code" />
          </el-select>
        </el-form-item>
        <el-form-item label="邀请有效期">
          <el-radio-group v-model="inviteForm.expireDays">
            <el-radio-button :value="7">7 天</el-radio-button>
            <el-radio-button :value="30">30 天</el-radio-button>
          </el-radio-group>
        </el-form-item>
      </el-form>
      <template #footer>
        <el-button @click="inviteVisible = false">取消</el-button>
        <el-button type="primary" :loading="saving" @click="submitInvite">生成邀请链接</el-button>
      </template>
    </el-dialog>
    <el-dialog
      v-model="inviteResultVisible"
      :title="createdInvite?.emailSent ? '邀请邮件已发送' : '邀请链接已生成'"
      width="600px"
      :close-on-click-modal="false"
      @closed="createdInvite = null"
    >
      <div v-if="createdInvite" class="result-wrap">
        <div class="ok-icon"><el-icon :size="26"><CircleCheckFilled /></el-icon></div>
        <div class="ok-title">{{ createdInvite.emailSent ? '邀请邮件已发送至 ' + (createdInvite.email || '') : createdInvite.roleName + '邀请链接已生成' }}</div>
        <div class="ok-sub">
          {{
            createdInvite.emailSent
              ? `对方收到邮件后点击“接受邀请并加入”即可进入邀请页，链接有效期至 ${formatTime(createdInvite.expireTime)}。`
              : `当前未配置发信邮箱，邮件未发送。请把链接发给被邀请人，对方完成注册后将自动加入企业。链接有效期至 ${formatTime(createdInvite.expireTime)}。`
          }}
        </div>
        <div v-if="createdInvite.emailSent" class="field-tip center-tip">如果对方未收到邮件，可在“邀请记录”中点击“重发邮件”。</div>
        <div class="link-box">
          <div class="mono link-text">{{ inviteUrl(createdInvite.inviteCode) }}</div>
          <el-button type="primary" plain size="small" @click="copyInviteUrl(createdInvite.inviteCode)">复制链接</el-button>
        </div>
      </div>
      <template #footer>
        <el-button @click="inviteResultVisible = false">完成</el-button>
      </template>
    </el-dialog>
  </div>
</template>
<script setup lang="ts">
import { computed, onMounted, reactive, ref } from 'vue'
import { ElMessage, ElMessageBox } from 'element-plus'
import { useUserStore } from '../../stores/user'
import OnboardingGuide from '../../components/OnboardingGuide.vue'
import {
  createMemberInvite,
  listMemberInvites,
  listMemberRoles,
  listMembers,
  resendMemberInvite,
  revokeMemberInvite,
  type CreateInviteResult,
  type InviteItem,
  type MemberItem,
  type MemberRoleOption,
} from '../../api/member'

const userStore = useUserStore()

const activeTab = ref('members')
const members = ref<MemberItem[]>([])
const invites = ref<InviteItem[]>([])
const roleOptions = ref<MemberRoleOption[]>([])
const loadingMembers = ref(false)
const loadingInvites = ref(false)
const saving = ref(false)
const resendingCode = ref('')
const inviteVisible = ref(false)
const inviteResultVisible = ref(false)
const createdInvite = ref<CreateInviteResult | null>(null)

const inviteForm = reactive({
  phone: '',
  email: '',
  roleCode: 'AGENT',
  expireDays: 7 as number,
})

const invitePendingCount = computed(() => invites.value.filter((i) => i.status === 1).length)

onMounted(async () => {
  try {
    if (!userStore.userId) await userStore.fetchProfile()
    if (userStore.userType === 2 && userStore.tenantCode === 'PLATFORM') {
      await userStore.refreshToken()
    }
  } catch {
    // 登录态由请求层统一处理
  }
  await Promise.all([loadMembers(), loadInvites(), loadRoles()])
})

function loadAll() {
  loadMembers()
  loadInvites()
}

async function loadRoles() {
  try {
    roleOptions.value = await listMemberRoles()
    if (roleOptions.value.length && !inviteForm.roleCode) {
      inviteForm.roleCode = roleOptions.value[0].code
    }
  } catch {
    roleOptions.value = []
  }
}

async function loadMembers() {
  loadingMembers.value = true
  try {
    members.value = await listMembers()
  } finally {
    loadingMembers.value = false
  }
}

async function loadInvites() {
  loadingInvites.value = true
  try {
    invites.value = await listMemberInvites()
  } finally {
    loadingInvites.value = false
  }
}

function openInviteDialog() {
  inviteForm.phone = ''
  inviteForm.email = ''
  inviteForm.expireDays = 7
  if (roleOptions.value.length) {
    inviteForm.roleCode = roleOptions.value[0].code
  }
  inviteVisible.value = true
}

async function submitInvite() {
  if (!inviteForm.email.trim()) {
    ElMessage.warning('请填写受邀邮箱')
    return
  }
  if (inviteForm.phone.trim() && !/^1[3-9]\d{9}$/.test(inviteForm.phone.trim())) {
    ElMessage.warning('手机号格式不正确')
    return
  }
  if (!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(inviteForm.email.trim())) {
    ElMessage.warning('邮箱格式不正确')
    return
  }
  saving.value = true
  try {
    createdInvite.value = await createMemberInvite({
      phone: inviteForm.phone.trim() || undefined,
      email: inviteForm.email.trim() || undefined,
      roleCode: inviteForm.roleCode,
      expireDays: inviteForm.expireDays,
    })
    inviteVisible.value = false
    inviteResultVisible.value = true
    await loadInvites()
    ElMessage.success(createdInvite.value?.emailSent ? '邀请邮件已发送' : '邀请链接已生成')
  } finally {
    saving.value = false
  }
}

async function resend(row: InviteItem) {
  resendingCode.value = row.inviteCode
  try {
    const sent = await resendMemberInvite(row.inviteCode)
    if (sent) {
      ElMessage.success(`邀请邮件已重新发送至 ${row.email || '受邀邮箱'}，请提醒对方查收`)
    } else {
      ElMessage.warning('邮件服务未启用，请先在“邀请记录”中复制链接发送给成员')
    }
  } catch {
    // 错误提示已由请求层统一处理
  } finally {
    resendingCode.value = ''
  }
}

function inviteUrl(code: string) {
  return `${window.location.origin}/invite/accept/${code}`
}

async function copyInviteUrl(code: string) {
  const text = inviteUrl(code)
  try {
    await navigator.clipboard.writeText(text)
    ElMessage.success('邀请链接已复制')
  } catch {
    ElMessage.warning('请手动复制链接')
  }
}

async function revoke(row: InviteItem) {
  await ElMessageBox.confirm(
    `撤销后链接将立即失效，「${row.email || row.phone || '该成员'}」将无法再加入，确认撤销吗？`,
    '撤销邀请',
    { type: 'warning', confirmButtonText: '确认撤销' },
  )
  await revokeMemberInvite(row.inviteCode)
  ElMessage.success('邀请已撤销')
  await loadInvites()
}

function roleTagType(code?: string) {
  if (code === 'SUPERVISOR') return 'warning'
  if (code === 'QUALITY' || code === 'AI_OPERATOR') return 'success'
  if (code === 'SENIOR_AGENT') return 'primary'
  return 'info'
}

function inviteTagType(status: number) {
  if (status === 1) return 'primary'
  if (status === 2) return 'success'
  return 'info'
}

function formatTime(time?: string) {
  return time ? time.replace('T', ' ').slice(0, 19) : '—'
}
</script>
<style scoped>
.settings-page { width: 100%; }
.page-title-row { display: flex; align-items: flex-start; justify-content: space-between; margin-bottom: 16px; }
.page-title { margin: 0; font-size: 20px; color: #0f172a; }
.page-sub { margin: 6px 0 0; font-size: 13px; color: #94a3b8; }
.btn-icon { margin-right: 4px; }

.stat-row { display: grid; grid-template-columns: repeat(3, minmax(160px, 240px)); gap: 14px; margin-bottom: 14px; }
.stat-card { background: #fff; border: 1px solid #e6e8ef; border-radius: 14px; padding: 16px 18px; }
.stat-value { font-size: 26px; font-weight: 800; color: #1d4ed8; line-height: 1.2; }
.stat-label { margin-top: 4px; font-size: 12px; color: #94a3b8; }

.main-card { width: 100%; }
.member-cell { display: flex; align-items: center; gap: 10px; }
.member-avatar { width: 36px; height: 36px; border-radius: 10px; color: #fff; display: flex; align-items: center; justify-content: center; font-weight: 700; flex-shrink: 0; background: linear-gradient(135deg, #1d4ed8, #3b82f6); box-shadow: 0 6px 14px rgba(29, 78, 216, .2); }
.member-name { display: flex; align-items: center; gap: 6px; font-weight: 600; color: #1e293b; }
.member-sub { color: #94a3b8; font-size: 12px; margin-top: 2px; }
.contact-line { color: #475569; font-size: 13px; }
.mono { font-family: ui-monospace, SFMono-Regular, Menlo, monospace; }
.full-width { width: 100%; }

.invite-tip { display: flex; align-items: center; gap: 8px; color: #475569; font-size: 13px; background: #f8fafc; border: 1px solid #eef2f7; border-radius: 10px; padding: 10px 14px; margin-bottom: 14px; }
.field-tip { font-size: 12px; color: #64748b; background: #f0f6ff; border: 1px solid #dbeafe; border-radius: 8px; padding: 8px 12px; margin-bottom: 14px; }

.result-wrap { text-align: center; padding: 4px 0 10px; }
.ok-icon { color: #16a34a; font-size: 36px; }
.ok-title { margin-top: 10px; font-size: 18px; font-weight: 700; color: #0f172a; }
.ok-sub { margin: 8px auto 16px; max-width: 460px; font-size: 13px; color: #64748b; line-height: 1.7; }
.link-box { display: flex; align-items: center; gap: 10px; background: #f8fafc; border: 1px solid #e6e8ef; border-radius: 10px; padding: 10px 12px; }
.link-text { flex: 1; text-align: left; color: #1e40af; font-size: 13px; word-break: break-all; }
.center-tip { text-align: center; margin: -4px 0 12px; }
</style>
```

### 5.5 公共接受邀请页

邀请邮件按钮指向 /invite/accept/{code}，该页面允许未登录用户读取邀请并完成注册：

### 文件：yunti-frontend/src/views/invite/accept.vue

``` code-block-container
<template>
  <div class="invite-page">
    <div class="invite-top">
      <div class="brand">
        <img class="brand-logo" src="/yunti-mark.svg" alt="云梯" />
        <span class="brand-name">云梯智能客服</span>
      </div>
    </div>
    <div class="invite-center">
      <div class="invite-card">
        <template v-if="loading">
          <div class="card-loading">
            <el-icon class="is-loading" :size="28"><Loading /></el-icon>
            <div class="loading-text">正在读取邀请信息…</div>
          </div>
        </template>
        <template v-else-if="invalid">
          <div class="status-icon invalid"><el-icon :size="30"><CircleCloseFilled /></el-icon></div>
          <div class="status-title">邀请链接不可用</div>
          <div class="status-sub">该邀请可能已被撤销、已过期，或链接地址有误。请联系企业管理员重新发送邀请。</div>
          <el-button type="primary" class="full-btn" @click="router.push('/login')">前往登录</el-button>
        </template>
        <template v-else-if="!accepted">
          <div class="card-head">
            <div class="head-badge">成员邀请</div>
            <div class="head-title">加入客服团队</div>
            <div class="head-sub">
              {{ preview?.inviterName ? `${preview.inviterName} 邀请你加入企业客服团队` : '你收到了企业客服团队邀请' }}
            </div>
            <div class="role-tag">
              <el-icon :size="14"><Avatar /></el-icon>
              受邀角色：{{ preview?.roleName || '客服专员' }}
            </div>
          </div>
          <el-form label-position="top" class="accept-form">
            <el-form-item label="姓名" required>
              <el-input v-model="form.name" maxlength="64" placeholder="请输入你的真实姓名" />
            </el-form-item>
            <div class="grid-2">
              <el-form-item label="手机号" :required="hasPhone">
                <el-input v-model="form.phone" maxlength="20" placeholder="用于登录与身份校验" />
              </el-form-item>
              <el-form-item label="邮箱" :required="hasEmail">
                <el-input v-model="form.email" maxlength="128" placeholder="用于登录与接收通知" />
              </el-form-item>
            </div>
            <div v-if="hasPhone || hasEmail" class="match-tip">
              <el-icon :size="14"><InfoFilled /></el-icon>
              <span>请填写管理员邀请时使用的{{ hasPhone ? '手机号' : '' }}{{ hasPhone && hasEmail ? '与' : '' }}{{ hasEmail ? '邮箱' : '' }}，填写不一致将无法完成加入。</span>
            </div>
            <el-form-item label="设置登录密码" required>
              <el-input v-model="form.password" type="password" show-password maxlength="64" placeholder="不少于 8 位，需同时包含字母和数字" />
            </el-form-item>
            <el-form-item label="确认密码" required>
              <el-input v-model="form.confirm" type="password" show-password maxlength="64" placeholder="再次输入密码" />
            </el-form-item>
          </el-form>
          <el-button type="primary" class="full-btn submit-btn" :loading="submitting" @click="submit">
            接受邀请并加入
          </el-button>
          <div class="expire-line">
            <el-icon :size="13"><Timer /></el-icon>
            <span>邀请有效期至 {{ formatTime(preview?.expireTime) }}</span>
          </div>
        </template>
        <template v-else>
          <div class="status-icon success"><el-icon :size="30"><CircleCheckFilled /></el-icon></div>
          <div class="status-title">加入成功 🎉</div>
          <div class="success-card">
            <div class="success-row"><span>登录账号</span><b class="mono">{{ result?.email || result?.phone || result?.userNo }}</b></div>
            <div class="success-row"><span>成员编号</span><b class="mono">{{ result?.userNo }}</b></div>
            <div class="success-row"><span>角色</span><b>{{ result?.roleName }}</b></div>
            <div class="success-row"><span>所属企业</span><b>{{ result?.tenantCode }}</b></div>
          </div>
          <div class="status-sub">账号已创建，请使用上方手机号 / 邮箱和你设置的密码登录客服工作台。</div>
          <el-button type="primary" class="full-btn" @click="goLogin">前往登录</el-button>
        </template>
      </div>
    </div>
    <div class="invite-foot">© 2026 云梯智能客服 · 多租户 SaaS 客服平台</div>
  </div>
</template>
<script setup lang="ts">
import { computed, onMounted, reactive, ref } from 'vue'
import { useRoute, useRouter } from 'vue-router'
import { ElMessage } from 'element-plus'
import { acceptMemberInvite, fetchInvitePreview, type AcceptResult, type InvitePreview } from '../../api/member'

const route = useRoute()
const router = useRouter()

const code = String(route.params.code || '')
const loading = ref(true)
const invalid = ref(false)
const accepted = ref(false)
const submitting = ref(false)
const preview = ref<InvitePreview | null>(null)
const result = ref<AcceptResult | null>(null)

const form = reactive({
  name: '',
  phone: '',
  email: '',
  password: '',
  confirm: '',
})

const hasPhone = computed(() => !!preview.value?.phoneMasked)
const hasEmail = computed(() => !!preview.value?.emailMasked)

onMounted(async () => {
  try {
    preview.value = await fetchInvitePreview(code)
  } catch {
    invalid.value = true
  } finally {
    loading.value = false
  }
})

async function submit() {
  if (!form.name.trim()) {
    ElMessage.warning('请填写姓名')
    return
  }
  if (hasPhone.value && !form.phone.trim()) {
    ElMessage.warning('请填写邀请时使用的手机号')
    return
  }
  if (hasEmail.value && !form.email.trim()) {
    ElMessage.warning('请填写邀请时使用的邮箱')
    return
  }
  if (!hasPhone.value && !hasEmail.value && !form.phone.trim() && !form.email.trim()) {
    ElMessage.warning('请填写手机号或邮箱')
    return
  }
  if (form.phone.trim() && !/^1[3-9]\d{9}$/.test(form.phone.trim())) {
    ElMessage.warning('手机号格式不正确')
    return
  }
  if (form.email.trim() && !/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(form.email.trim())) {
    ElMessage.warning('邮箱格式不正确')
    return
  }
  if (!/^(?=.*[A-Za-z])(?=.*\d).{8,64}$/.test(form.password)) {
    ElMessage.warning('密码需不少于 8 位，且同时包含字母和数字')
    return
  }
  if (form.password !== form.confirm) {
    ElMessage.warning('两次输入的密码不一致')
    return
  }
  submitting.value = true
  try {
    result.value = await acceptMemberInvite(code, {
      name: form.name.trim(),
      password: form.password,
      phone: form.phone.trim() || undefined,
      email: form.email.trim() || undefined,
    })
    accepted.value = true
  } finally {
    submitting.value = false
  }
}

function goLogin() {
  router.push('/login')
}

function formatTime(time?: string) {
  return time ? time.replace('T', ' ').slice(0, 19) : '—'
}
</script>
<style scoped>
.invite-page { min-height: 100vh; display: flex; flex-direction: column; background: linear-gradient(180deg, #f5f8ff 0%, #eef4ff 42%, #f8fbff 100%); }
.invite-top { padding: 24px 36px; }
.brand { display: flex; align-items: center; gap: 10px; }
.brand-logo { width: 34px; height: 34px; }
.brand-name { font-size: 18px; font-weight: 800; color: #0b1220; letter-spacing: .2px; }
.invite-center { flex: 1; display: flex; align-items: flex-start; justify-content: center; padding: 18px 16px 30px; }
.invite-card { width: 460px; max-width: 100%; background: #fff; border-radius: 18px; box-shadow: 0 20px 60px rgba(30, 64, 175, .12); border: 1px solid #e3ebfa; padding: 30px 34px 24px; }
.card-head { text-align: center; margin-bottom: 22px; }
.head-badge { display: inline-block; color: #2563eb; background: #eff6ff; border: 1px solid #dbeafe; border-radius: 999px; font-size: 12px; padding: 4px 12px; }
.head-title { font-size: 22px; font-weight: 800; color: #0f172a; margin-top: 12px; }
.head-sub { margin-top: 6px; color: #64748b; font-size: 13px; }
.role-tag { display: inline-flex; align-items: center; gap: 6px; margin-top: 14px; color: #1d4ed8; background: #eef4ff; border: 1px solid #dbeafe; border-radius: 8px; font-size: 13px; font-weight: 600; padding: 6px 12px; }
.accept-form { text-align: left; }
.grid-2 { display: grid; grid-template-columns: 1fr 1fr; gap: 12px; }
.match-tip { display: flex; align-items: flex-start; gap: 6px; background: #fffbeb; border: 1px solid #fde68a; border-radius: 8px; color: #92400e; font-size: 12px; padding: 8px 10px; margin: -4px 0 16px; line-height: 1.6; }
.full-btn { width: 100%; }
.submit-btn { height: 42px; margin-top: 4px; }
.expire-line { display: flex; align-items: center; justify-content: center; gap: 6px; margin-top: 14px; color: #94a3b8; font-size: 12px; }
.status-icon { width: 64px; height: 64px; border-radius: 50%; display: flex; align-items: center; justify-content: center; margin: 14px auto 16px; }
.status-icon.success { color: #16a34a; background: #f0fdf4; border: 1px solid #bbf7d0; }
.status-icon.invalid { color: #dc2626; background: #fef2f2; border: 1px solid #fecaca; }
.status-title { font-size: 20px; font-weight: 800; color: #0f172a; text-align: center; }
.status-sub { margin: 10px auto 20px; max-width: 360px; color: #64748b; font-size: 13px; line-height: 1.8; text-align: center; }
.success-card { margin: 18px 0; background: #f8fafc; border: 1px solid #e6e8ef; border-radius: 12px; padding: 6px 16px; }
.success-row { display: flex; justify-content: space-between; padding: 10px 0; color: #475569; font-size: 13px; }
.success-row + .success-row { border-top: 1px dashed #e2e8f0; }
.success-row b { color: #0f172a; }
.mono { font-family: ui-monospace, SFMono-Regular, Menlo, monospace; }
.card-loading { text-align: center; padding: 50px 0; color: #2563eb; }
.loading-text { margin-top: 12px; color: #64748b; font-size: 13px; }
.invite-foot { text-align: center; padding: 16px; color: #94a3b8; font-size: 12px; }
</style>
```

### 5.6 路由（整体替换）

### 文件：yunti-frontend/src/router/index.ts

``` code-block-container
import { createRouter, createWebHistory } from 'vue-router'
import { getToken } from '../utils/auth'

const router = createRouter({
  history: createWebHistory(),
  routes: [
    {
      path: '/login',
      name: 'Login',
      component: () => import('../views/login/index.vue'),
      meta: { title: '登录' },
    },
    {
      path: '/register',
      name: 'Register',
      component: () => import('../views/register/index.vue'),
      meta: { title: '注册企业账号' },
    },
    {
      path: '/',
      component: () => import('../layouts/AdminLayout.vue'),
      redirect: '/entry',
      children: [
        {
          path: 'dashboard',
          name: 'Dashboard',
          component: () => import('../views/dashboard/index.vue'),
          meta: { title: '数据概览' },
        },
        {
          path: 'enterprise',
          name: 'EnterpriseReview',
          component: () => import('../views/enterprise/index.vue'),
          meta: { title: '企业信息' },
        },
        {
          path: 'admin/reviews',
          name: 'PlatformReview',
          component: () => import('../views/admin/review/index.vue'),
          meta: { title: '企业审核', requiresPlatform: true },
        },
        {
          path: 'profile',
          name: 'Profile',
          component: () => import('../views/profile/index.vue'),
          meta: { title: '个人中心' },
        },
        {
          path: 'channels',
          name: 'Channels',
          component: () => import('../views/channels/index.vue'),
          meta: { title: '渠道接入' },
        },
        {
          path: 'channels/setup/:stage/:channelId?',
          name: 'ChannelSetup',
          component: () => import('../views/channels/setup.vue'),
          meta: { title: '渠道接入流程' },
        },
        {
          path: 'modules/bot',
          name: 'BotConfig',
          component: () => import('../views/bot/index.vue'),
          meta: { title: '智能机器人' },
        },
        {
          path: 'members',
          name: 'TeamMembers',
          component: () => import('../views/members/index.vue'),
          meta: { title: '成员管理' },
        },
        {
          path: 'modules/settings',
          redirect: '/members',
        },
        ...[
          ['workspace', '在线客服'],
          ['kb', '知识库'],
          ['tickets', '工单管理'],
          ['customers', '客户管理'],
          ['qa', '质检中心'],
          ['analytics', '数据报表'],
          ['upgrade', '版本与升级'],
          ['orders', '我的订单'],
          ['support', '支持工单'],
        ].map(([path, title]) => ({
          path: `modules/${path}`,
          name: `Module${String(path).charAt(0).toUpperCase()}${String(path).slice(1)}`,
          component: () => import('../views/module/index.vue'),
          meta: { title },
        })),
      ],
    },
    {
      path: '/entry',
      name: 'Entry',
      component: () => import('../views/entry/index.vue'),
      meta: { title: '进入后台' },
    },
    {
      path: '/guide',
      name: 'Guide',
      component: () => import('../views/guide/index.vue'),
      meta: { title: '企业开通引导' },
    },
    {
      path: '/invite/accept/:code',
      name: 'InviteAccept',
      component: () => import('../views/invite/accept.vue'),
      meta: { title: '接受客服成员邀请', public: true },
    },
    {
      path: '/:pathMatch(.*)*',
      name: 'NotFound',
      component: () => import('../views/error/404.vue'),
    },
  ],
})

// 简单路由守卫：未登录跳登录页
router.beforeEach((to) => {
  if (to.meta.public) {
    return true
  }
  if (!['Login', 'Register'].includes(String(to.name)) && !getToken()) {
    return { name: 'Login', query: { redirect: to.fullPath } }
  }
  return true
})

export default router
```

### 5.7 后台布局（整体替换）

### 文件：yunti-frontend/src/layouts/AdminLayout.vue

``` code-block-container
<template>
  <el-container class="admin-layout">
    <el-aside width="220px" class="admin-aside">
      <div class="admin-logo">
        <img class="admin-logo-img" src="/yunti-mark.svg" alt="云梯" />
        <span>云梯智能客服</span>
      </div>
      <el-menu
        router
        :default-active="$route.path"
        class="admin-menu"
        background-color="#0b1220"
        text-color="#8a97ad"
        active-text-color="#ffffff"
      >
        <el-menu-item index="/dashboard">
          <el-icon><DataBoard /></el-icon>
          <span>数据概览</span>
        </el-menu-item>
        <el-menu-item v-if="userStore.userType === 1" index="/admin/reviews">
          <el-icon><Stamp /></el-icon>
          <span>企业审核</span>
        </el-menu-item>
        <template v-if="userStore.userType === 2">
          <el-menu-item v-if="canManageMembers" index="/enterprise">
            <el-icon><OfficeBuilding /></el-icon>
            <span>企业信息</span>
            <span v-if="tipFor('enterprise')" class="menu-tip" :class="tipFor('enterprise').type">{{ tipFor('enterprise').text }}</span>
          </el-menu-item>
          <el-sub-menu index="workbench">
            <template #title>
              <span class="group-label">工作台</span>
            </template>
            <el-menu-item index="/modules/workspace"><el-icon><ChatDotRound /></el-icon><span>在线客服</span></el-menu-item>
            <el-menu-item index="/modules/bot"><el-icon><Cpu /></el-icon><span>智能机器人</span><span v-if="tipFor('bot')" class="menu-tip" :class="tipFor('bot').type">{{ tipFor('bot').text }}</span></el-menu-item>
          </el-sub-menu>
          <el-sub-menu v-if="canManageMembers" index="team">
            <template #title>
              <span class="group-label">团队管理</span>
            </template>
            <el-menu-item index="/members"><el-icon><UserFilled /></el-icon><span>成员管理</span><span v-if="tipFor('member')" class="menu-tip" :class="tipFor('member').type">{{ tipFor('member').text }}</span></el-menu-item>
          </el-sub-menu>
          <el-sub-menu index="ops">
            <template #title><span class="group-label">运营管理</span></template>
            <el-menu-item index="/modules/kb"><el-icon><Notebook /></el-icon><span>知识库</span></el-menu-item>
            <el-menu-item index="/modules/tickets"><el-icon><Tickets /></el-icon><span>工单管理</span></el-menu-item>
            <el-menu-item index="/modules/customers"><el-icon><UserFilled /></el-icon><span>客户管理</span></el-menu-item>
            <el-menu-item index="/modules/qa"><el-icon><DataAnalysis /></el-icon><span>质检中心</span><span v-if="tipFor('qa')" class="menu-tip" :class="tipFor('qa').type">{{ tipFor('qa').text }}</span></el-menu-item>
          </el-sub-menu>
          <el-sub-menu index="config">
            <template #title><span class="group-label">数据与配置</span></template>
            <el-menu-item index="/modules/analytics"><el-icon><Histogram /></el-icon><span>数据报表</span></el-menu-item>
            <el-menu-item index="/channels"><el-icon><Connection /></el-icon><span>渠道接入</span><span v-if="tipFor('channel')" class="menu-tip" :class="tipFor('channel').type">{{ tipFor('channel').text }}</span></el-menu-item>
            <el-menu-item index="/modules/upgrade"><el-icon><Promotion /></el-icon><span>版本与升级</span></el-menu-item>
            <el-menu-item index="/modules/orders"><el-icon><Wallet /></el-icon><span>我的订单</span></el-menu-item>
            <el-menu-item index="/modules/support"><el-icon><Headset /></el-icon><span>支持工单</span></el-menu-item>
          </el-sub-menu>
        </template>
      </el-menu>
    </el-aside>
    <el-container>
      <el-header class="admin-header">
        <el-breadcrumb separator="/">
          <el-breadcrumb-item :to="{ path: '/dashboard' }">首页</el-breadcrumb-item>
          <el-breadcrumb-item>{{ $route.meta.title || '工作台' }}</el-breadcrumb-item>
        </el-breadcrumb>
        <div class="tb-actions">
          <!-- 消息中心 -->
          <el-tooltip content="消息中心" placement="bottom">
            <button class="icon-btn" @click="notifyOpen = true">
              <el-icon :size="18"><Bell /></el-icon>
              <span v-if="unreadCount > 0" class="notif-count">{{ unreadCount > 99 ? '99+' : unreadCount }}</span>
            </button>
          </el-tooltip>
          <span class="tb-divider"></span>
          <!-- 用户菜单 -->
          <el-dropdown trigger="click" @command="handleUserCommand">
            <div class="tb-user">
              <div class="avatar-sm" :class="avatarClass">
                {{ avatarText }}
                <span class="st online"></span>
              </div>
              <div class="user-text">
                <div class="user-name">{{ userStore.name || '未登录' }}</div>
                <div class="user-role">{{ roleText }}</div>
              </div>
              <el-icon class="caret"><ArrowDown /></el-icon>
            </div>
            <template #dropdown>
              <el-dropdown-menu class="user-menu">
                <div class="drop-head">
                  <div class="drop-name">{{ userStore.name }}</div>
                  <div class="drop-no mono">{{ userStore.userNo }}</div>
                </div>
                <el-dropdown-item command="profile">
                  <el-icon><User /></el-icon>
                  个人中心
                </el-dropdown-item>
                <el-dropdown-item command="password">
                  <el-icon><Lock /></el-icon>
                  修改密码
                </el-dropdown-item>
                <el-dropdown-item command="logout" divided class="drop-logout">
                  <el-icon><SwitchButton /></el-icon>
                  退出登录
                </el-dropdown-item>
              </el-dropdown-menu>
            </template>
          </el-dropdown>
        </div>
      </el-header>
      <el-main class="admin-main">
        <router-view />
      </el-main>
    </el-container>
    <!-- 消息中心抽屉 -->
    <el-drawer v-model="notifyOpen" title="消息中心" size="440px" class="notify-drawer">
      <div class="notify-tabs">
        <button
          v-for="tab in notifyTabs"
          :key="tab.key"
          class="notify-tab"
          :class="{ active: notifyTab === tab.key }"
          @click="notifyTab = tab.key"
        >
          {{ tab.label }}
          <span v-if="tabCount(tab.key)" class="tab-count">{{ tabCount(tab.key) }}</span>
        </button>
      </div>
      <div class="notify-list">
        <div
          v-for="item in filteredNotices"
          :key="item.id"
          class="notify-item"
          :class="{ unread: !item.read }"
          @click="markRead(item)"
        >
          <div class="notify-icon" :class="`type-${item.type}`">
            <el-icon :size="16">
              <component :is="iconOf(item.type)" />
            </el-icon>
          </div>
          <div class="notify-body">
            <div class="notify-title">
              {{ item.title }}
              <span v-if="!item.read" class="unread-dot"></span>
            </div>
            <div class="notify-text">{{ item.text }}</div>
            <div class="notify-time">{{ item.time }}</div>
          </div>
        </div>
        <el-empty v-if="!filteredNotices.length" description="暂无消息" :image-size="90" />
      </div>
      <template #footer>
        <div class="notify-footer">
          <span class="muted-text">共 {{ totalCount }} 条 · {{ unreadCount }} 条未读</span>
          <el-button size="small" :disabled="unreadCount === 0" @click="markAllRead">
            全部已读
          </el-button>
        </div>
      </template>
    </el-drawer>
    <!-- 修改密码 -->
    <ChangePasswordDialog v-model="pwdVisible" />

    <!-- 退出登录确认 -->
    <LogoutConfirmDialog
      v-model="logoutVisible"
      :name="userStore.name || ''"
      :account="userStore.userNo || ''"
      @confirm="doLogout"
    />
  </el-container>
</template>
<script setup lang="ts">
import { computed, onMounted, reactive, ref } from 'vue'
import { useRouter } from 'vue-router'
import { ElMessage } from 'element-plus'
import { getEnterpriseGuide } from '../api/enterprise'
import { listChannels } from '../api/customer/channel'
import { fetchMemberAccess } from '../api/member'
import { useUserStore } from '../stores/user'
import ChangePasswordDialog from '../components/ChangePasswordDialog.vue'
import LogoutConfirmDialog from '../components/LogoutConfirmDialog.vue'

interface NoticeItem {
  id: string
  type: 'system' | 'review' | 'qa'
  title: string
  text: string
  time: string
  read: boolean
}

const router = useRouter()
const userStore = useUserStore()

const ONBOARD_DONE_KEY = 'yunti_onboard_done'
const NOTIFY_READ_KEY = 'yunti_notify_read'

const notifyOpen = ref(false)
const pwdVisible = ref(false)
const logoutVisible = ref(false)
const guideStage = ref('')
const channelRunning = ref(false)
const canManageMembers = ref(false)
const currentRoleName = ref('')
const notifyTab = ref<'all' | 'system' | 'review' | 'qa'>('all')

const notifyTabs = [
  { key: 'all' as const, label: '全部' },
  { key: 'system' as const, label: '系统通知' },
  { key: 'review' as const, label: '审核通知' },
  { key: 'qa' as const, label: '质检通知' },
]

const roleText = computed(() => {
  if (userStore.userType === 1) return '平台管理员'
  if (userStore.userType === 2) return currentRoleName.value || '企业成员'
  return '企业成员'
})

const avatarText = computed(() => (userStore.name || '用').slice(0, 1))

const avatarClass = computed(() => {
  const name = userStore.name || ''
  let hash = 0
  for (let i = 0; i < name.length; i++) {
    hash = (hash * 31 + name.charCodeAt(i)) >>> 0
  }
  return `avatar-hue-${hash % 5}`
})

const notices = reactive<NoticeItem[]>([
  {
    id: 'n1',
    type: 'system',
    title: '平台服务升级通知',
    text: '系统将于 9 月 6 日 02:00-04:00 升级，期间接入渠道可能出现短暂中断。',
    time: '今天 18:20',
    read: false,
  },
  {
    id: 'n2',
    type: 'review',
    title: '企业资料审核结果',
    text: '企业资料已通过审核，云梯客服租户已开通。',
    time: '今天 16:08',
    read: false,
  },
  {
    id: 'n3',
    type: 'qa',
    title: '今日质检报告已生成',
    text: '昨日共质检 286 条会话，发现 3 条待跟进的服务风险，请及时处理。',
    time: '今天 10:30',
    read: false,
  },
  {
    id: 'n4',
    type: 'system',
    title: '渠道密钥安全提醒',
    text: '近期出现渠道密钥泄露风险，请勿将密钥明文发送给第三方。',
    time: '昨天 20:12',
    read: true,
  },
  {
    id: 'n5',
    type: 'qa',
    title: '质检规则生效',
    text: '「服务态度」质检规则已更新并即时生效。',
    time: '昨天 14:02',
    read: true,
  },
])

const readIds = ref<Set<string>>(new Set(loadReadIds()))

const filteredNotices = computed(() => {
  if (notifyTab.value === 'all') return notices
  return notices.filter((n) => n.type === notifyTab.value)
})

const unreadCount = computed(() => notices.filter((n) => !n.read).length)
const totalCount = computed(() => notices.length)

onMounted(async () => {
  applyReadIds()
  try {
    if (!userStore.userId) {
      await userStore.fetchProfile()
    }
    if (userStore.userType === 1) return
    if (userStore.userType === 2) {
      try {
        const access = await fetchMemberAccess()
        canManageMembers.value = access.canManage
        currentRoleName.value = access.roleName || '企业成员'
      } catch {
        // 老版本后端未提供权限接口时，兼容为企业管理员
        canManageMembers.value = true
        currentRoleName.value = '企业管理员'
      }
    }
    if (!canManageMembers.value) {
      // 普通客服成员不进入企业开通引导，也不展示企业信息/成员管理
      return
    }
    const guide = await getEnterpriseGuide()
    guideStage.value = guide.stage
    let done = localStorage.getItem(ONBOARD_DONE_KEY) === 'true'
    if (guide.stage === 'APPROVED') {
      try {
        const channels = await listChannels()
        channelRunning.value = channels.some((c) => c.stage === 3)
        if (channelRunning.value) {
          done = true
          localStorage.setItem(ONBOARD_DONE_KEY, 'true')
        }
      } catch {
        // 渠道服务不可用时按本地标记处理
      }
    }
    if (
      router.currentRoute.value.name !== 'EnterpriseReview'
      && (guide.stage !== 'APPROVED' || !done)
    ) {
      router.replace('/guide')
    }
  } catch {
    // 状态查询失败时保持可访问，避免后台不可用造成死循环
  }
})

function doneFlag(key: string) {
  return localStorage.getItem(`yunti_ob_done_${key}`) === 'true'
}

function tipFor(key: string): { text: string; type: string } {
  if (userStore.userType !== 2) return { text: '', type: '' }
  if (key === 'enterprise') {
    if (guideStage.value === 'PENDING_PROFILE' || guideStage.value === 'REJECTED') {
      return { text: '待完善', type: 'danger' }
    }
    if (guideStage.value === 'PENDING_REVIEW') {
      return { text: '审核中', type: 'warning' }
    }
    return { text: '', type: '' }
  }
  if (guideStage.value !== 'APPROVED') return { text: '', type: '' }
  if (key === 'channel' && !channelRunning.value) {
    return { text: '待接入', type: 'warning' }
  }
  if (key === 'bot' && channelRunning.value && !doneFlag('bot')) {
    return { text: '待配置', type: 'warning' }
  }
  if (key === 'member' && channelRunning.value && !doneFlag('member')) {
    return { text: '待配置', type: 'warning' }
  }
  if (key === 'qa' && channelRunning.value && !doneFlag('qa')) {
    return { text: '待配置', type: 'warning' }
  }
  return { text: '', type: '' }
}

function handleUserCommand(command: string | number | object) {
  if (command === 'profile') {
    router.push('/profile')
  } else if (command === 'password') {
    pwdVisible.value = true
  } else if (command === 'logout') {
    openLogoutConfirm()
  }
}

async function openLogoutConfirm() {
  // 页面刷新后先恢复会话，确保弹窗展示真实姓名与账号
  if (!userStore.userId) {
    try {
      await userStore.fetchProfile()
    } catch {
      // 拉取失败时按原信息弹窗，不阻断退出操作
    }
  }
  logoutVisible.value = true
}

function doLogout() {
  userStore.reset()
  localStorage.removeItem(ONBOARD_DONE_KEY)
  ElMessage.success('已安全退出登录')
  router.push('/login')
}

function iconOf(type: NoticeItem['type']) {
  if (type === 'review') return 'CircleCheck'
  if (type === 'qa') return 'DataAnalysis'
  return 'Bell'
}

function tabCount(key: string) {
  if (key === 'all') return unreadCount.value
  return notices.filter((n) => n.type === key && !n.read).length
}

function markRead(item: NoticeItem) {
  if (!item.read) {
    item.read = true
    persistReadIds()
  }
}

function markAllRead() {
  notices.forEach((n) => {
    n.read = true
  })
  persistReadIds()
}

function applyReadIds() {
  notices.forEach((n) => {
    if (readIds.value.has(n.id)) {
      n.read = true
    }
  })
}

function persistReadIds() {
  localStorage.setItem(NOTIFY_READ_KEY, JSON.stringify(notices.filter((n) => n.read).map((n) => n.id)))
}

function loadReadIds(): string[] {
  try {
    const raw = localStorage.getItem(NOTIFY_READ_KEY)
    return raw ? (JSON.parse(raw) as string[]) : []
  } catch {
    return []
  }
}
</script>
<style scoped>
.admin-layout {
  height: 100%;
}

.admin-aside {
  background: #0b1220;
  display: flex;
  flex-direction: column;
}

.admin-logo {
  height: 60px;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 8px;
  color: #fff;
  font-size: 16px;
  font-weight: 700;
  border-bottom: 1px solid rgba(148, 163, 184, 0.12);
}

.admin-logo-img {
  width: 26px;
  height: 26px;
  border-radius: 7px;
}

.admin-menu {
  border-right: none;
  flex: 1;
}

.admin-menu :deep(.el-menu-item) {
  display: flex;
  align-items: center;
}

.menu-tip {
  margin-left: auto;
  font-size: 11px;
  line-height: 1;
  padding: 3px 7px;
  border-radius: 999px;
  flex-shrink: 0;
}

.menu-tip.warning {
  color: #fbbf24;
  background: rgba(245, 158, 11, 0.16);
  animation: menuTipPulse 1.2s ease-in-out infinite;
}

.menu-tip.danger {
  color: #f87171;
  background: rgba(239, 68, 68, 0.16);
  animation: menuTipPulse 1s ease-in-out infinite;
}

@keyframes menuTipPulse {
  0%,
  100% {
    opacity: 1;
    transform: scale(1);
  }
  50% {
    opacity: 0.55;
    transform: scale(0.92);
  }
}

.admin-header {
  height: 60px;
  background: #fff;
  border-bottom: 1px solid #e6e8ef;
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 0 20px;
}

.tb-actions {
  display: flex;
  align-items: center;
  gap: 6px;
}

.icon-btn {
  position: relative;
  width: 36px;
  height: 36px;
  border: none;
  border-radius: 10px;
  background: transparent;
  color: #475569;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  cursor: pointer;
  transition: all 0.18s;
}

.icon-btn:hover {
  background: #f1f5f9;
  color: #2563eb;
}

.notif-count {
  position: absolute;
  top: 2px;
  right: 0;
  min-width: 16px;
  height: 16px;
  padding: 0 4px;
  border-radius: 999px;
  background: #ef4444;
  color: #fff;
  font-size: 10px;
  line-height: 16px;
  text-align: center;
  border: 2px solid #fff;
}

.tb-divider {
  width: 1px;
  height: 22px;
  background: #e6e8ef;
  margin: 0 10px;
}

.tb-user {
  display: flex;
  align-items: center;
  gap: 10px;
  padding: 6px 8px;
  border-radius: 12px;
  cursor: pointer;
  transition: background 0.18s;
  outline: none;
}

.tb-user:hover {
  background: #f1f5f9;
}

.avatar-sm {
  position: relative;
  width: 36px;
  height: 36px;
  border-radius: 50%;
  color: #fff;
  font-size: 15px;
  font-weight: 700;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}

.avatar-sm .st {
  position: absolute;
  right: 0;
  bottom: 1px;
  width: 9px;
  height: 9px;
  border-radius: 50%;
  background: #10b981;
  border: 2px solid #fff;
}

.user-text {
  line-height: 1.25;
  min-width: 0;
}

.user-name {
  font-size: 13px;
  font-weight: 600;
  color: #1e293b;
  max-width: 120px;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.user-role {
  font-size: 11px;
  color: #94a3b8;
  margin-top: 1px;
}

.caret {
  color: #94a3b8;
  font-size: 12px;
}

.admin-main {
  background: #f5f6f8;
  padding: 20px;
}

.user-menu :deep(.drop-head) {
  padding: 10px 14px 12px;
  border-bottom: 1px solid #eef2f7;
}

.user-menu :deep(.drop-name) {
  font-size: 14px;
  font-weight: 700;
  color: #0f172a;
}

.user-menu :deep(.drop-no) {
  font-size: 11px;
  color: #94a3b8;
  margin-top: 4px;
}

.user-menu :deep(.el-dropdown-menu__item) {
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 9px 16px;
}

.user-menu :deep(.drop-logout) {
  color: #dc2626;
}

.user-menu :deep(.drop-logout:hover) {
  color: #b91c1c;
  background: #fef2f2;
}

.avatar-hue-0 { background: linear-gradient(135deg, #2563eb, #38bdf8); }
.avatar-hue-1 { background: linear-gradient(135deg, #1d4ed8, #60a5fa); }
.avatar-hue-2 { background: linear-gradient(135deg, #0284c7, #38bdf8); }
.avatar-hue-3 { background: linear-gradient(135deg, #3b82f6, #93c5fd); }
.avatar-hue-4 { background: linear-gradient(135deg, #1e40af, #3b82f6); }

.notify-drawer :deep(.el-drawer__header) {
  margin-bottom: 4px;
  font-weight: 700;
}

.notify-tabs {
  display: flex;
  gap: 6px;
  padding: 10px 0 14px;
  border-bottom: 1px solid #eef2f7;
}

.notify-tab {
  position: relative;
  border: none;
  background: #f1f5f9;
  color: #64748b;
  font-size: 12.5px;
  font-weight: 600;
  padding: 6px 12px;
  border-radius: 999px;
  cursor: pointer;
  transition: all 0.18s;
}

.notify-tab.active {
  background: #eff6ff;
  color: #2563eb;
}

.notify-tab .tab-count {
  margin-left: 4px;
  color: #2563eb;
  font-size: 11px;
}

.notify-list {
  padding: 8px 0;
  min-height: 300px;
}

.notify-item {
  display: flex;
  align-items: flex-start;
  gap: 12px;
  padding: 14px 10px;
  border-radius: 12px;
  cursor: pointer;
  transition: background 0.15s;
}

.notify-item:hover {
  background: #f8fafc;
}

.notify-icon {
  width: 34px;
  height: 34px;
  border-radius: 10px;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}

.type-system { background: #eff6ff; color: #2563eb; }
.type-review { background: #e0f2fe; color: #0284c7; }
.type-qa { background: #dbeafe; color: #1d4ed8; }

.notify-body {
  flex: 1;
  min-width: 0;
}

.notify-title {
  font-size: 13.5px;
  font-weight: 600;
  color: #1e293b;
  display: flex;
  align-items: center;
  gap: 6px;
}

.notify-item.read .notify-title {
  color: #64748b;
  font-weight: 500;
}

.unread-dot {
  width: 7px;
  height: 7px;
  border-radius: 50%;
  background: #ef4444;
  flex-shrink: 0;
}

.notify-text {
  font-size: 12.5px;
  color: #64748b;
  line-height: 1.7;
  margin-top: 4px;
}

.notify-time {
  font-size: 11px;
  color: #cbd5e1;
  margin-top: 6px;
}

.notify-footer {
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.muted-text {
  font-size: 12px;
  color: #94a3b8;
}

.mono {
  font-family: ui-monospace, SFMono-Regular, Menlo, monospace;
}
</style>
<style>
/* 下拉菜单 / 抽屉为 teleport 渲染，需使用非 scoped 样式 */
.user-menu .el-dropdown-menu__item {
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 9px 16px;
}

.user-menu .drop-head {
  padding: 10px 14px 12px;
  border-bottom: 1px solid #eef2f7;
  margin-bottom: 4px;
}

.user-menu .drop-name {
  font-size: 14px;
  font-weight: 700;
  color: #0f172a;
}

.user-menu .drop-no {
  font-size: 11px;
  color: #94a3b8;
  margin-top: 4px;
}

.user-menu .drop-logout {
  color: #dc2626;
}

.user-menu .drop-logout:hover {
  color: #b91c1c;
  background: #fef2f2;
}

.notify-drawer .el-drawer__header {
  margin-bottom: 4px;
  font-weight: 700;
}

.notify-drawer .el-drawer__body {
  padding-top: 0;
}

.logout-confirm {
  width: 400px;
}
</style>
```

### 5.8 开通引导卡片（整体替换）

### 文件：yunti-frontend/src/components/OnboardingGuide.vue

``` code-block-container
<template>
  <div v-if="userStore.userType === 2 && canManage && !hidden && !allDone" class="ob-card">
    <div class="ob-head">
      <div>
        <div class="ob-title">🎉 系统初始化 · 完成以下 4 步即可开始接待客户</div>
        <div class="ob-sub">已完成 {{ doneCount }}/{{ steps.length }} 步</div>
      </div>
      <div class="ob-right">
        <el-progress
          type="circle"
          :percentage="Math.round((doneCount / steps.length) * 100)"
          :width="38"
          :stroke-width="4"
        />
        <button class="ob-close" aria-label="关闭" @click="hidden = true">
          <el-icon :size="16"><Close /></el-icon>
        </button>
      </div>
    </div>
    <div class="ob-list">
      <div
        v-for="(step, i) in steps"
        :key="step.id"
        class="ob-step"
        :class="{ done: step.done, current: i === currentIndex && !step.done }"
      >
        <span class="ob-no">
          <el-icon v-if="step.done"><Check /></el-icon>
          <template v-else>{{ i + 1 }}</template>
        </span>
        <div class="ob-info">
          <div class="ob-name">
            {{ step.label }}
            <el-tag
              size="small"
              :type="step.done ? 'success' : step.id === 'channel' ? 'primary' : 'info'"
              effect="light"
            >{{ step.done ? '已完成' : step.id === 'channel' ? '当前步骤' : '待完成' }}</el-tag>
          </div>
          <div class="ob-desc">{{ step.desc }}</div>
        </div>
        <el-button v-if="!step.done" size="small" :type="i === currentIndex ? 'primary' : 'default'" @click="go(step)">
          {{ step.id === 'channel' ? '去接入' : '去配置' }}
        </el-button>
        <el-button v-else size="small" text @click="go(step)">查看</el-button>
      </div>
    </div>
    <div class="ob-foot">
      <span class="ob-tip">渠道步骤在渠道上线后自动完成，其余步骤完成配置后可手动标记。</span>
    </div>
  </div>
</template>
<script setup lang="ts">
import { computed, onMounted, ref } from 'vue'
import { useRouter } from 'vue-router'
import { listChannels } from '../api/customer/channel'
import { fetchMemberAccess } from '../api/member'
import { useUserStore } from '../stores/user'

const router = useRouter()
const userStore = useUserStore()
const emit = defineEmits<{ (e: 'refresh'): void }>()
const HIDE_KEY = 'yunti_onboard_guide_hidden'
const DONE_PREFIX = 'yunti_ob_done_'
const hidden = ref(localStorage.getItem(HIDE_KEY) === 'true')
const canManage = ref(false)

const steps = ref([
  { id: 'channel', label: '接入第一个渠道', desc: '官网 / 微信 / 小程序任选其一', path: '/channels', done: false },
  { id: 'bot', label: '配置智能机器人', desc: '开启意图识别与多轮对话', path: '/modules/bot', done: false },
  { id: 'member', label: '邀请客服成员', desc: '创建坐席账号与技能组', path: '/members', done: false },
  { id: 'qa', label: '发起首次质检', desc: 'AI 全量初检今日会话', path: '/modules/qa', done: false },
])

const doneCount = computed(() => steps.value.filter((s) => s.done).length)
const allDone = computed(() => doneCount.value === steps.value.length)
const currentIndex = computed(() => steps.value.findIndex((s) => !s.done))

onMounted(async () => {
  try {
    if (userStore.userType === 2) {
      const access = await fetchMemberAccess()
      canManage.value = access.canManage
    }
  } catch {
    // 老版本后端无权限接口时兼容为企业管理员展示
    canManage.value = userStore.userType === 2
  }
  if (!canManage.value) {
    return
  }
  steps.value.forEach((s) => {
    s.done = localStorage.getItem(DONE_PREFIX + s.id) === 'true'
  })
  try {
    const channels = await listChannels()
    const running = channels.some((c) => c.stage === 3)
    if (running) {
      steps.value[0].done = true
      localStorage.setItem(DONE_PREFIX + 'channel', 'true')
    }
  } catch {
    // 渠道状态不可用时按本地记录展示
  }
})

function go(step: { id: string; path: string; done: boolean }) {
  if (step.id !== 'channel' && !step.done) {
    localStorage.setItem(DONE_PREFIX + step.id, 'true')
    step.done = true
  }
  if (router.currentRoute.value.path === step.path) {
    emit('refresh')
    return
  }
  router.push(step.path)
}
</script>
<style scoped>
.ob-card {
  background: linear-gradient(135deg, #eff6ff, #f8fbff);
  border: 1px solid #dbeafe;
  border-radius: 16px;
  padding: 12px 16px;
  margin-bottom: 14px;
}

.ob-head {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 14px;
}

.ob-right {
  display: flex;
  align-items: center;
  gap: 10px;
  flex-shrink: 0;
}

.ob-close {
  width: 24px;
  height: 24px;
  border: none;
  border-radius: 8px;
  background: transparent;
  color: #94a3b8;
  cursor: pointer;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  transition: all 0.18s;
}

.ob-close:hover {
  background: #eff6ff;
  color: #2563eb;
}

.ob-title {
  font-size: 14px;
  font-weight: 700;
  color: #0f172a;
}

.ob-sub {
  font-size: 11.5px;
  color: #64748b;
  margin-top: 2px;
}

.ob-list {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  margin-top: 8px;
  gap: 8px;
}

.ob-step {
  display: flex;
  align-items: center;
  gap: 12px;
  background: #fff;
  border: 1px solid #e6e8ef;
  border-radius: 12px;
  padding: 6px 12px;
}

.ob-step.done {
  background: #f0fdf4;
  border-color: #bbf7d0;
}

.ob-step.current {
  border-color: #2563eb;
  box-shadow: 0 0 0 3px rgba(37, 99, 235, 0.08);
}

.ob-no {
  width: 22px;
  height: 22px;
  border-radius: 50%;
  background: #e2e8f0;
  color: #64748b;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 12px;
  font-weight: 700;
  flex-shrink: 0;
}

.ob-step.done .ob-no {
  background: #bbf7d0;
  color: #16a34a;
}

.ob-step.current .ob-no {
  background: #2563eb;
  color: #fff;
  box-shadow: 0 0 0 4px rgba(37, 99, 235, 0.14);
}

.ob-info {
  flex: 1;
  min-width: 0;
}

.ob-name {
  font-size: 13px;
  font-weight: 600;
  color: #1e293b;
  display: flex;
  align-items: center;
  gap: 8px;
  flex-wrap: wrap;
}

.ob-desc {
  font-size: 11.5px;
  color: #94a3b8;
  margin-top: 2px;
}

.ob-foot {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 10px;
  margin-top: 8px;
}

.ob-tip {
  color: #94a3b8;
  font-size: 11.5px;
}

@media (max-width: 900px) {
  .ob-list {
    grid-template-columns: 1fr;
  }
}
</style>
```

### 5.9 企业信息页（整体替换）

### 文件：yunti-frontend/src/views/enterprise/index.vue

``` code-block-container
<template>
  <div class="enterprise-page">
    <div class="page-title-row">
      <div>
        <h2 class="page-title">我的企业</h2>
        <p class="page-sub">查看企业资料与开通审核状态</p>
      </div>
      <el-button v-if="guide?.stage === 'PENDING_PROFILE' || guide?.stage === 'REJECTED'" type="primary" @click="router.push('/guide')">
        去完善企业信息
      </el-button>
    </div>
    <div v-if="loading" class="loading-card">
      <el-icon class="is-loading" :size="26"><Loading /></el-icon>
      <p>正在加载企业信息…</p>
    </div>
    <el-alert
      v-else-if="error"
      :title="error"
      type="error"
      show-icon
      :closable="false"
      class="error-alert"
    />

    <template v-else-if="guide">
      <el-card shadow="never" class="status-card">
        <template #header>
          <span class="card-title">企业开通状态</span>
        </template>
        <div class="status-body">
          <div class="status-icon" :class="stageMeta.type">
            <el-icon :size="30"><component :is="stageMeta.icon" /></el-icon>
          </div>
          <div class="status-info">
            <div class="status-title">{{ stageMeta.title }}</div>
            <div class="status-sub">{{ stageMeta.desc }}</div>
            <div v-if="guide.tenantCode" class="tenant-code mono">租户编码：{{ guide.tenantCode }}</div>
          </div>
          <el-tag :type="stageMeta.tagType" effect="light" size="large">{{ stageMeta.badge }}</el-tag>
        </div>
      </el-card>
      <div class="info-grid">
        <el-card shadow="never">
          <template #header><span class="card-title">企业资料</span></template>
          <div class="info-list">
            <div class="info-row"><span class="k">企业名称</span><span class="v">{{ guide.companyName || '—' }}</span></div>
            <div class="info-row"><span class="k">统一社会信用代码</span><span class="v">{{ guide.licenseNo || '—' }}</span></div>
            <div class="info-row"><span class="k">注册地址</span><span class="v">{{ guide.registerAddress || '—' }}</span></div>
            <div class="info-row"><span class="k">法人代表</span><span class="v">{{ guide.legalPerson || '—' }}</span></div>
            <div class="info-row"><span class="k">所属行业 / 规模</span><span class="v">{{ guide.industry || '—' }} / {{ guide.scale || '—' }}</span></div>
            <div class="info-row"><span class="k">管理员</span><span class="v">{{ guide.contactName || '—' }}</span></div>
          </div>
        </el-card>
        <el-card shadow="never">
          <template #header><span class="card-title">审核与营业执照</span></template>
          <div class="info-list">
            <div class="info-row"><span class="k">企业编码</span><span class="v mono">{{ guide.enterpriseCode }}</span></div>
            <div class="info-row"><span class="k">申请单号</span><span class="v mono">{{ guide.applyNo || '—' }}</span></div>
            <div class="info-row"><span class="k">提交版本</span><span class="v">第 {{ guide.versionNo ?? '—' }} 次申请</span></div>
            <div class="info-row">
              <span class="k">营业执照</span>
              <span v-if="licenseFileName" class="v file-cell">
                <a v-if="licensePreview" class="file-link" :href="licensePreview" target="_blank">预览 {{ licenseFileName }}</a>
                <template v-else>{{ licenseFileName }}</template>
              </span>
              <span v-else class="v">未上传</span>
            </div>
            <div v-if="guide.stage === 'REJECTED'" class="reject-box">
              <div class="reject-title">驳回原因</div>
              <div class="reject-text">{{ guide.rejectReason || '资料核验未通过，请修改后重新提交' }}</div>
            </div>
          </div>
        </el-card>
      </div>
    </template>
  </div>
</template>
<script setup lang="ts">
import { computed, onMounted, ref } from 'vue'
import { useRouter } from 'vue-router'
import { fetchEnterpriseLicense, getEnterpriseGuide } from '../../api/enterprise'
import { fetchMemberAccess } from '../../api/member'
import type { EnterpriseGuideState } from '../../types'

const router = useRouter()
const loading = ref(true)
const error = ref('')
const guide = ref<EnterpriseGuideState | null>(null)
const licenseFileName = ref('')
const licensePreview = ref('')

const stageMeta = computed(() => {
  const stage = guide.value?.stage
  if (stage === 'PENDING_REVIEW') {
    return { type: 'pending', icon: 'Clock', title: '企业资料审核中', desc: '运营正在核验营业执照与法人信息，预计 1~2 个工作日内完成', badge: '审核中', tagType: 'warning' as const }
  }
  if (stage === 'REJECTED') {
    return { type: 'rejected', icon: 'CircleCloseFilled', title: '审核未通过', desc: '请根据驳回原因修改资料后重新提交', badge: '已驳回', tagType: 'danger' as const }
  }
  if (stage === 'APPROVED') {
    return { type: 'approved', icon: 'CircleCheckFilled', title: '企业已开通', desc: '审核已通过，云梯客服租户已就绪', badge: '已开通', tagType: 'success' as const }
  }
  return { type: 'todo', icon: 'EditPen', title: '企业资料待完善', desc: '完成企业资料填写后提交运营审核', badge: '待完善', tagType: 'info' as const }
})

onMounted(load)

async function load() {
  loading.value = true
  error.value = ''
  try {
    const access = await fetchMemberAccess()
    if (!access.canManage) {
      // 普通客服成员不展示企业信息，直接回到工作台
      router.replace('/dashboard')
      return
    }
    guide.value = await getEnterpriseGuide()
    if (guide.value?.licenseFileId) {
      try {
        const blob = await fetchEnterpriseLicense(guide.value.licenseFileId)
        if (blob.type.startsWith('image/')) {
          licensePreview.value = URL.createObjectURL(blob)
        }
        licenseFileName.value = `营业执照附件 (${Math.ceil(blob.size / 1024)} KB)`
      } catch {
        licenseFileName.value = '已上传'
      }
    }
  } catch (e) {
    error.value = e instanceof Error ? e.message : '加载失败'
  } finally {
    loading.value = false
  }
}
</script>
<style scoped>
.enterprise-page {
  width: 100%;
  min-width: 0;
}

.page-title-row {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: 14px;
  margin-bottom: 18px;
}

.page-title {
  margin: 0;
  font-size: 20px;
  color: #0f172a;
}

.page-sub {
  margin: 6px 0 0;
  font-size: 13px;
  color: #94a3b8;
}

.loading-card {
  text-align: center;
  padding: 50px 0;
  color: #64748b;
}

.error-alert {
  margin-bottom: 16px;
}

.status-card {
  margin-bottom: 18px;
}

.card-title {
  font-size: 15px;
  font-weight: 700;
  color: #1e293b;
}

.status-body {
  display: flex;
  align-items: center;
  gap: 16px;
}

.status-icon {
  width: 60px;
  height: 60px;
  border-radius: 16px;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}

.status-icon.pending { background: #fff7ed; color: #f59e0b; }
.status-icon.rejected { background: #fef2f2; color: #dc2626; }
.status-icon.approved { background: #ecfdf5; color: #10b981; }
.status-icon.todo { background: #eff6ff; color: #2563eb; }

.status-info {
  flex: 1;
}

.status-title {
  font-size: 18px;
  font-weight: 700;
  color: #0f172a;
}

.status-sub {
  font-size: 13px;
  color: #64748b;
  margin-top: 5px;
  line-height: 1.7;
}

.tenant-code {
  margin-top: 8px;
  color: #2563eb;
  font-size: 13px;
}

.info-grid {
  display: grid;
  grid-template-columns: 1.2fr 1fr;
  gap: 20px;
}

.info-list {
  display: flex;
  flex-direction: column;
}

.info-row {
  display: flex;
  justify-content: space-between;
  gap: 16px;
  padding: 11px 0;
  border-bottom: 1px dashed #eef2f7;
  font-size: 13px;
}

.info-row:last-child {
  border-bottom: none;
}

.info-row .k {
  color: #94a3b8;
  flex-shrink: 0;
}

.info-row .v {
  color: #1e293b;
  font-weight: 500;
  text-align: right;
  word-break: break-all;
}

.mono {
  font-family: ui-monospace, SFMono-Regular, Menlo, monospace;
}

.file-cell {
  display: inline-flex;
  align-items: center;
  gap: 6px;
}

.file-link {
  color: #2563eb;
  text-decoration: none;
}

.file-link:hover {
  text-decoration: underline;
}

.reject-box {
  margin-top: 12px;
  background: #fef2f2;
  border: 1px solid #fecaca;
  border-radius: 10px;
  padding: 10px 12px;
}

.reject-title {
  font-size: 12.5px;
  font-weight: 700;
  color: #b91c1c;
}

.reject-text {
  font-size: 12.5px;
  color: #dc2626;
  margin-top: 4px;
  line-height: 1.6;
}

@media (max-width: 820px) {
  .info-grid {
    grid-template-columns: 1fr;
  }
}
</style>
```

## 六、运行与验证

### 6.1 数据库

``` code-block-container
cd /path/to/customer-service
psql -h 127.0.0.1 -U mac -d user_db -f schema/user_db_member_invite.sql
```

### 6.2 后端

``` code-block-container
cd yunti-backend
mvn -pl yunti-user-service spring-boot:run      # 9091
```

如果只想验证邀请流程而不真正发邮件，可以不配置 SMTP（此时创建邀请返回 emailSent=false，可复制链接测试）。要真实发邮件，启动前配置：

``` code-block-container
export MAIL_HOST=smtp.163.com
export MAIL_PORT=465
export MAIL_USERNAME=你的发信邮箱
export MAIL_PASSWORD=SMTP授权码
export YUNTI_MEMBER_FRONTEND_URL=http://localhost:5174
```

### 6.3 前端

``` code-block-container
cd yunti-frontend
npm run dev        # http://localhost:5174
```

### 6.4 验证步骤

1.  企业管理员登录，进入“团队管理 → 成员管理”；

  

<img src="https://article-images.zsxq.com/FkiLRqlRUwuR_IrnS_6MdAht1KS-" class="tiptap-image" alt="图片.png" />

1.  点击“邀请成员”，填写受邀邮箱、角色、有效期；

      

<img src="https://article-images.zsxq.com/FgwT3ahUlL62I3foAOwFBdw5O3Cn" class="tiptap-image" alt="图片.png" />

1.  创建成功后可复制邀请链接或重发邮件；

      

<img src="https://article-images.zsxq.com/Fmh3ukm469rIr18-Iw4U0f6U5nNd" class="tiptap-image" alt="图片.png" />

1.  新窗口打开 /invite/accept/{inviteCode}，填写姓名、密码完成注册；

      

<img src="https://article-images.zsxq.com/Fr3B4Aa2XAZi16GpE8H94Y5sK91W" class="tiptap-image" alt="图片.png" />

1.  回到成员管理，看到新成员与“已加入”的邀请状态；

      

<img src="https://article-images.zsxq.com/FnB_1yxn-UexpbIe3tHFJCzOMMaF" class="tiptap-image" alt="图片.png" />

  

<img src="https://article-images.zsxq.com/FjX-tcusZjNERi6m3YFPbYKEnxgJ" class="tiptap-image" alt="图片.png" />

1.  普通客服成员登录后，不应看到企业信息、成员管理与开通引导。

## 总结

本文实现了从“管理员发起邀请 → 邮件送达 → 被邀请人注册加入 → 成员列表可管理”的完整闭环。

角色、成员关系、邀请记录均按 tenant\_code 隔离；

成员权限收敛到企业管理员，普通客服只使用业务工作台。

后续可在本基础上继续实现技能组、坐席在线状态与人工会话分配。
