---
title: "26 客户 360 视图：客户画像、标签体系与会话轨迹（四）"
source: "https://articles.zsxq.com/id_ea9ezptbi54z.html"
author:
  - "[[苏三]]"
published:
created: 2026-09-25
description:
tags:
  - "clippings"
---
[来自： Java突击队&AI项目实战](https://wx.zsxq.com/group/28851182188851)

### 7.3 标签体系页面

### 文件：yunti-frontend/src/views/customers/tags.vue

标签体系：按分组摆开标签卡片（编码、类型、已打客户数、排序、命中条件），管理员可新建/编辑/启停/删除；规则标签在弹窗里配"指标 + 比较符 + 阈值 + 窗口"，并实时预览成人话（"投诉类会话数 ≥ 2（近 30 天）"）。页面顶部还有「重算全部客户」——改了阈值必须按一次，存量客户才会回到新口径。

``` code-block-container
<template>
  <div class="tag-page">
    <div class="page-title-row">
      <div>
        <h2 class="page-title">标签体系</h2>
        <p class="page-sub">
          标签先定义、再打标：分组 / 颜色 / 手工或规则 / 排序都在这里维护，客户 360 里打的就是这套标签
        </p>
        <p v-if="access" class="role-line">
          当前角色：<b>{{ access.roleName }}</b>
          <span v-if="access.hint">· {{ access.hint }}</span>
        </p>
      </div>
      <div class="head-actions">
        <el-button @click="router.push('/modules/customers')">
          <el-icon class="btn-icon"><Back /></el-icon>
          客户 360
        </el-button>
        <el-button :loading="loading" @click="load">
          <el-icon class="btn-icon"><Refresh /></el-icon>
          刷新
        </el-button>
        <el-button v-if="access?.canManageTag" :loading="recalculating" @click="recalculate">
          <el-icon class="btn-icon"><MagicStick /></el-icon>
          重算全部客户
        </el-button>
        <el-button v-if="access?.canManageTag" type="primary" @click="openCreate">
          <el-icon class="btn-icon"><Plus /></el-icon>
          新建标签
        </el-button>
      </div>
    </div>
    <div class="stat-row">
      <div class="stat-card">
        <div class="stat-icon blue"><el-icon :size="18"><PriceTag /></el-icon></div>
        <div><div class="stat-value">{{ rows.length }}</div><div class="stat-label">标签总数</div></div>
      </div>
      <div class="stat-card">
        <div class="stat-icon green"><el-icon :size="18"><Select /></el-icon></div>
        <div><div class="stat-value">{{ enabledCount }}</div><div class="stat-label">启用中</div></div>
      </div>
      <div class="stat-card">
        <div class="stat-icon purple"><el-icon :size="18"><MagicStick /></el-icon></div>
        <div><div class="stat-value">{{ autoCount }}</div><div class="stat-label">规则自动</div></div>
      </div>
      <div class="stat-card">
        <div class="stat-icon amber"><el-icon :size="18"><UserFilled /></el-icon></div>
        <div><div class="stat-value">{{ usedCount }}</div><div class="stat-label">已使用</div></div>
      </div>
    </div>
    <el-empty v-if="!loading && !rows.length" description="还没有标签，先建几个（例如：高价值、待回访、风险客户）" />

    <div v-for="group in groups" :key="group.name" class="group-block">
      <div class="group-head">
        <span class="group-name">{{ group.name }}</span>
        <span class="group-count">{{ group.tags.length }} 个标签</span>
      </div>
      <div class="tag-grid">
        <div v-for="tag in group.tags" :key="tag.id" class="tag-card" :class="{ disabled: !tag.enabled }">
          <div class="tc-top">
            <el-tag size="small" effect="dark" :type="tagTypeOf(tag.color)">{{ tag.tagName }}</el-tag>
            <el-tag v-if="!tag.enabled" size="small" type="info" effect="plain">已停用</el-tag>
            <span class="tc-code mono">{{ tag.tagCode }}</span>
          </div>
          <div class="tc-desc">{{ tag.description || tag.ruleHint || '—' }}</div>
          <div class="tc-meta">
            <span>{{ tag.tagTypeText }}</span>
            <span class="dot">·</span>
            <span>已打 {{ tag.customerCount }} 位客户</span>
            <span class="dot">·</span>
            <span>排序 {{ tag.sortNo }}</span>
          </div>
          <div v-if="tag.ruleText" class="tc-rule">
            <el-icon :size="12"><MagicStick /></el-icon>
            命中条件：{{ tag.ruleText }}
          </div>
          <div v-else-if="tag.tagType === 2" class="tc-rule muted">
            还没配可执行条件（引擎不会自动跑这个标签）
          </div>
          <div v-if="access?.canManageTag" class="tc-actions">
            <el-button link type="primary" @click="openEdit(tag)">编辑</el-button>
            <el-button link type="primary" @click="toggle(tag)">{{ tag.enabled ? '停用' : '启用' }}</el-button>
            <el-button link type="danger" @click="remove(tag)">删除</el-button>
          </div>
          <div v-else class="tc-actions muted">只读：标签体系由企业管理员维护</div>
        </div>
      </div>
    </div>
    <el-dialog v-model="formVisible" :title="form.id ? '编辑标签' : '新建标签'" width="560px">
      <el-form label-width="96px">
        <el-form-item label="标签名称" required>
          <el-input v-model="form.tagName" maxlength="32" show-word-limit placeholder="例如：高价值客户" />
        </el-form-item>
        <el-form-item label="标签编码">
          <el-input v-model="form.tagCode" maxlength="32" placeholder="留空自动生成，例如：VIP_HIGH" />
        </el-form-item>
        <el-form-item label="分组" required>
          <el-select v-model="form.tagGroup" class="full-width">
            <el-option v-for="item in groupNames" :key="item" :value="item" :label="item" />
          </el-select>
        </el-form-item>
        <el-form-item label="展示颜色">
          <el-select v-model="form.color" class="full-width">
            <el-option v-for="item in colors" :key="item" :value="item" :label="item" />
          </el-select>
        </el-form-item>
        <el-form-item label="标签类型">
          <el-radio-group v-model="form.tagType">
            <el-radio-button :value="1">手工打标</el-radio-button>
            <el-radio-button :value="2">规则自动</el-radio-button>
          </el-radio-group>
        </el-form-item>
        <!-- 规则标签的四件套：指标 + 比较符 + 阈值 + 统计窗口。配了它引擎才会真的自己跑 -->
        <template v-if="form.tagType === 2">
          <el-form-item label="规则指标">
            <el-select v-model="form.ruleMetric" class="full-width" clearable placeholder="选择一个指标（选完引擎才会跑）">
              <el-option v-for="item in metrics" :key="item.value" :value="item.value" :label="item.label" />
            </el-select>
          </el-form-item>
          <el-form-item label="比较条件">
            <div class="rule-row">
              <el-select v-model="form.ruleOp" class="w-120" placeholder="比较符">
                <el-option value="GT" label="大于 >" />
                <el-option value="GTE" label="大于等于 ≥" />
                <el-option value="LT" label="小于 <" />
                <el-option value="LTE" label="小于等于 ≤" />
                <el-option value="EQ" label="等于 =" />
              </el-select>
              <el-input-number v-model="form.ruleValue" :precision="2" :step="1" class="w-160" />
              <el-input-number
                v-if="isSessionMetric"
                v-model="form.ruleWindowDays"
                :min="0"
                :max="3650"
                class="w-160"
              />
              <span v-if="isSessionMetric" class="rule-unit">天统计窗口（0 = 全周期）</span>
            </div>
          </el-form-item>
          <el-form-item label="规则预览">
            <span class="rule-preview">{{ rulePreview }}</span>
          </el-form-item>
        </template>
        <el-form-item v-if="form.tagType === 2" label="命中口径">
          <el-input
            v-model="form.ruleHint"
            maxlength="255"
            placeholder="例如：近 30 天投诉类会话 ≥ 2 次（写给人看的说明；真正的条件是上面的指标 + 比较符 + 阈值）"
          />
        </el-form-item>
        <el-form-item label="标签说明">
          <el-input v-model="form.description" maxlength="255" show-word-limit placeholder="这个标签用来干什么" />
        </el-form-item>
        <el-form-item label="排序号">
          <el-input-number v-model="form.sortNo" :min="0" :max="9999" />
        </el-form-item>
        <el-form-item label="是否启用">
          <el-switch v-model="form.enabled" />
        </el-form-item>
      </el-form>
      <div class="dialog-tip">
        停用后不能再打这个标签，但已经打上的客户仍然展示——历史数据不会因为停用而消失。
      </div>
      <template #footer>
        <el-button @click="formVisible = false">取消</el-button>
        <el-button type="primary" :loading="saving" @click="submit">保存</el-button>
      </template>
    </el-dialog>
  </div>
</template>
<script setup lang="ts">
import { computed, onMounted, reactive, ref } from 'vue'
import { useRouter } from 'vue-router'
import { ElMessage, ElMessageBox } from 'element-plus'
import {
  createTagDef,
  deleteTagDef,
  fetchCustomerAccess,
  fetchMetricOptions,
  fetchTagDefs,
  recalculateAllTags,
  tagTypeOf,
  toggleTagDef,
  updateTagDef,
  type CustomerAccess,
  type MetricOption,
  type TagDef,
} from '../../api/customer/customer'

const router = useRouter()

const loading = ref(false)
const saving = ref(false)
const recalculating = ref(false)
const rows = ref<TagDef[]>([])
const access = ref<CustomerAccess | null>(null)
const metrics = ref<MetricOption[]>([])

const groupNames = ['价值', '服务', '风险', '偏好', '来源', '其它']
const colors = ['blue', 'green', 'orange', 'red', 'purple', 'gray']

const formVisible = ref(false)
const form = reactive({
  id: '',
  tagCode: '',
  tagName: '',
  tagGroup: '价值',
  color: 'blue',
  tagType: 1,
  ruleHint: '',
  ruleMetric: '',
  ruleOp: 'GTE',
  ruleValue: 0,
  ruleWindowDays: 0,
  description: '',
  sortNo: 100,
  enabled: true,
})

const enabledCount = computed(() => rows.value.filter((row) => row.enabled).length)
const autoCount = computed(() => rows.value.filter((row) => row.tagType === 2).length)
const usedCount = computed(() => rows.value.filter((row) => row.customerCount > 0).length)

/** 会话类指标才有"统计窗口"（档案类指标是全周期的，窗口对它没意义） */
const SESSION_METRICS = ['REFUND_SESSIONS', 'COMPLAINT_SESSIONS', 'NIGHT_SESSIONS']
const isSessionMetric = computed(() => SESSION_METRICS.includes(form.ruleMetric))

/** 规则预览：把四件套拼成人话，配错了当场能看出来 */
const rulePreview = computed(() => {
  if (!form.ruleMetric) {
    return '选一个指标，引擎才会自动跑这个标签'
  }
  const metric = metrics.value.find((item) => item.value === form.ruleMetric)?.label ?? form.ruleMetric
  const opText: Record<string, string> = { GT: ' > ', GTE: ' ≥ ', LT: ' < ', LTE: ' ≤ ', EQ: ' = ' }
  const scope = isSessionMetric.value && form.ruleWindowDays > 0
    ? `（近 ${form.ruleWindowDays} 天）` : '（全周期）'
  return `${metric}${opText[form.ruleOp] ?? ' ? '}${form.ruleValue}${scope}`
})

/** 按分组排开：标签体系页的"分组"要能一眼看全，而不是一长条列表 */
const groups = computed(() => {
  const map = new Map<string, TagDef[]>()
  for (const row of rows.value) {
    map.set(row.tagGroup, [...(map.get(row.tagGroup) ?? []), row])
  }
  return [...map.entries()].map(([name, tags]) => ({ name, tags }))
})

onMounted(() => {
  void load()
})

async function load() {
  loading.value = true
  try {
    const [defs, currentAccess, metricOptions] = await Promise.all([
      fetchTagDefs(),
      fetchCustomerAccess(),
      fetchMetricOptions(),
    ])
    rows.value = defs
    access.value = currentAccess
    metrics.value = metricOptions
  } finally {
    loading.value = false
  }
}

/** 全量重算：改了阈值之后必须按一次，存量客户才会回到新口径 */
async function recalculate() {
  recalculating.value = true
  try {
    const result = await recalculateAllTags()
    ElMessage.success(`已重算 ${result.scanned} 位客户：新打标 ${result.tagged} 次、摘标 ${result.untagged} 次`)
    await load()
  } finally {
    recalculating.value = false
  }
}

function openCreate() {
  form.id = ''
  form.tagCode = ''
  form.tagName = ''
  form.tagGroup = '价值'
  form.color = 'blue'
  form.tagType = 1
  form.ruleHint = ''
  form.ruleMetric = ''
  form.ruleOp = 'GTE'
  form.ruleValue = 0
  form.ruleWindowDays = 0
  form.description = ''
  form.sortNo = 100
  form.enabled = true
  formVisible.value = true
}

function openEdit(tag: TagDef) {
  form.id = tag.id
  form.tagCode = tag.tagCode
  form.tagName = tag.tagName
  form.tagGroup = tag.tagGroup
  form.color = tag.color
  form.tagType = tag.tagType
  form.ruleHint = tag.ruleHint ?? ''
  form.ruleMetric = tag.ruleMetric ?? ''
  form.ruleOp = tag.ruleOp ?? 'GTE'
  form.ruleValue = Number(tag.ruleValue ?? 0)
  form.ruleWindowDays = tag.ruleWindowDays ?? 0
  form.description = tag.description ?? ''
  form.sortNo = tag.sortNo
  form.enabled = tag.enabled
  formVisible.value = true
}

async function submit() {
  if (!form.tagName.trim()) {
    ElMessage.warning('标签名称不能为空')
    return
  }
  saving.value = true
  try {
    const body = {
      tagCode: form.tagCode.trim() || undefined,
      tagName: form.tagName.trim(),
      tagGroup: form.tagGroup,
      color: form.color,
      tagType: form.tagType,
      ruleHint: form.ruleHint,
      ruleMetric: form.tagType === 2 && form.ruleMetric ? form.ruleMetric : undefined,
      ruleOp: form.tagType === 2 && form.ruleMetric ? form.ruleOp : undefined,
      ruleValue: form.tagType === 2 && form.ruleMetric ? form.ruleValue : undefined,
      ruleWindowDays: form.tagType === 2 && form.ruleMetric ? form.ruleWindowDays : undefined,
      description: form.description,
      sortNo: form.sortNo,
      enabled: form.enabled,
    }
    if (form.id) {
      await updateTagDef(form.id, body)
      ElMessage.success('标签已更新')
    } else {
      await createTagDef(body)
      ElMessage.success('标签已创建')
    }
    formVisible.value = false
    await load()
  } finally {
    saving.value = false
  }
}

async function toggle(tag: TagDef) {
  await toggleTagDef(tag.id, !tag.enabled)
  ElMessage.success(tag.enabled ? `已停用「${tag.tagName}」` : `已启用「${tag.tagName}」`)
  await load()
}

async function remove(tag: TagDef) {
  if (tag.customerCount > 0) {
    ElMessage.warning(`还有 ${tag.customerCount} 位客户打着「${tag.tagName}」，先摘掉标签或改成停用`)
    return
  }
  await ElMessageBox.confirm(
    `确定删除标签「${tag.tagName}」吗？删除后不能再给客户打这个标签。`,
    '删除标签',
    { type: 'warning', confirmButtonText: '删除', cancelButtonText: '取消' },
  )
  await deleteTagDef(tag.id)
  ElMessage.success('标签已删除')
  await load()
}
</script>
<style scoped>
.tag-page { width: 100%; }
.page-title-row { display: flex; align-items: flex-start; justify-content: space-between; margin-bottom: 16px; gap: 12px; }
.page-title { margin: 0; font-size: 20px; color: #0f172a; }
.page-sub { margin: 6px 0 0; font-size: 13px; color: #94a3b8; }
.role-line { margin: 6px 0 0; font-size: 12px; color: #94a3b8; }
.role-line b { color: #2563eb; }
.head-actions { display: flex; align-items: center; gap: 12px; }
.btn-icon { margin-right: 4px; }
.stat-row { display: grid; grid-template-columns: repeat(4, 1fr); gap: 12px; margin-bottom: 16px; }
.stat-card { display: flex; align-items: center; gap: 12px; background: #fff; border: 1px solid #e6e8ef; border-radius: 14px; padding: 14px 16px; }
.stat-icon { width: 38px; height: 38px; border-radius: 10px; display: flex; align-items: center; justify-content: center; flex-shrink: 0; }
.stat-icon.blue { background: #eef4ff; color: #2563eb; }
.stat-icon.amber { background: #fff7ed; color: #d97706; }
.stat-icon.green { background: #ecfdf5; color: #059669; }
.stat-icon.purple { background: #f5f3ff; color: #7c3aed; }
.stat-value { font-size: 22px; font-weight: 800; color: #0f172a; line-height: 1.2; }
.stat-label { margin-top: 3px; font-size: 12px; color: #94a3b8; }
.group-block { margin-bottom: 20px; }
.group-head { display: flex; align-items: center; gap: 8px; margin-bottom: 10px; }
.group-name { font-size: 15px; font-weight: 700; color: #0f172a; }
.group-count { font-size: 12px; color: #94a3b8; }
.tag-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(280px, 1fr)); gap: 12px; }
.tag-card { background: #fff; border: 1px solid #e6e8ef; border-radius: 12px; padding: 14px 16px; }
.tag-card.disabled { background: #fafafa; }
.tc-top { display: flex; align-items: center; gap: 8px; }
.tc-code { color: #94a3b8; margin-left: auto; }
.tc-desc { margin-top: 10px; font-size: 13px; color: #475569; line-height: 1.7; min-height: 22px; }
.tc-meta { margin-top: 8px; font-size: 12px; color: #94a3b8; }
.tc-meta .dot { margin: 0 4px; }
.tc-actions { margin-top: 10px; display: flex; align-items: center; gap: 4px; }
.tc-actions.muted { font-size: 12px; }
.tc-rule { margin-top: 8px; display: flex; align-items: center; gap: 4px; padding: 6px 8px; border-radius: 6px; background: #f5f3ff; color: #6d28d9; font-size: 12px; }
.rule-row { display: flex; align-items: center; gap: 8px; flex-wrap: wrap; }
.rule-unit { font-size: 12px; color: #94a3b8; }
.rule-preview { font-size: 13px; color: #6d28d9; }
.w-120 { width: 120px; }
.w-160 { width: 160px; }
.muted { color: #94a3b8; }
.mono { font-family: ui-monospace, SFMono-Regular, Menlo, monospace; font-size: 12px; }
.full-width { width: 100%; }
.dialog-tip { margin-top: 4px; padding: 9px 12px; border-radius: 8px; background: #f8fafc; border: 1px solid #eef2f7; color: #64748b; font-size: 12px; line-height: 1.7; }
</style>
```

### 7.4 路由与菜单

### 改动：yunti-frontend/src/router/index.ts

改动点：新增客户 360 与标签体系两条路由（`/modules/customers`、`/modules/customers/tags`）

这个文件一共 2 处改动，按每处的定位说明直接改（代码块给的都是「改完之后」的样子）：

**改动 1（第 123 行附近）** 定位 `meta: { title: '工单中心' },`，在它后面加上：

``` code-block-container
          meta: { title: '工单中心' },
        },
        {
          // 客户 360：客户列表；点进去是画像 + 标签体系 + 会话轨迹
          path: 'modules/customers',
          name: 'Customers',
          component: () => import('../views/customers/index.vue'),
          meta: { title: '客户 360' },
        },
        {
          path: 'modules/customers/tags',
          name: 'CustomerTags',
          component: () => import('../views/customers/tags.vue'),
          meta: { title: '标签体系' },
        },
        {
          path: 'modules/workspace',
          name: 'Workspace',
          component: () => import('../views/workspace/index.vue'),
```

**改动 2（第 143 行附近）** 定位 `['kb', '知识库'],`，把它连同下面这些一起删掉（删完的样子如下）：

``` code-block-container
        },
        ...[
          ['kb', '知识库'],
          ['analytics', '数据报表'],
          ['upgrade', '版本与升级'],
          ['orders', '我的订单'],
```

### 改动：yunti-frontend/src/layouts/AdminLayout.vue

改动点：左侧「运营管理」里把原来的占位菜单换成「客户 360」

这个文件一共 1 处改动，按每处的定位说明直接改（代码块给的都是「改完之后」的样子）：

**改动 1（第 58 行附近）** 定位 `{{ ticketOverdue ? `超时 $ {ticketOverdue}`:`待办 ${ticketTodo}` }}`，把它这一处改成下面这样：

``` code-block-container
                {{ ticketOverdue ? `超时 ${ticketOverdue}` : `待办 ${ticketTodo}` }}
              </span>
            </el-menu-item>
            <el-menu-item index="/modules/customers"><el-icon><UserFilled /></el-icon><span>客户 360</span></el-menu-item>
            <el-menu-item index="/modules/qa"><el-icon><DataAnalysis /></el-icon><span>质检中心</span><span v-if="tipFor('qa')" class="menu-tip" :class="tipFor('qa').type">{{ tipFor('qa').text }}</span></el-menu-item>
            <el-menu-item index="/modules/qa/alerts"><el-icon><BellFilled /></el-icon><span>实时预警</span><span v-if="alertPending" class="menu-tip warning">{{ alertPending }}</span></el-menu-item>
          </el-sub-menu>
```

### 7.5 与其它页面的联动

四个小改动，让"接单前先看人"落到坐席的日常动作里：

### 改动：yunti-frontend/src/views/workspace/index.vue

改动点：会话头部新增「客户 360」入口（按客户编号跳页；没有关联档案时禁用）

这一条让"接单前先看人"落到坐席的日常动作里。

这个文件一共 2 处改动，按每处的定位说明直接改（代码块给的都是「改完之后」的样子）：

**改动 1（第 200 行附近）** 定位 `<el-icon :size="13"><Tickets /></el-icon>`，在它后面加上：

``` code-block-container
                <el-icon :size="13"><Tickets /></el-icon>
                转工单
              </el-button>
              <!-- 客户 360：接单前先看这个人是谁、之前发生过什么（按客户编号跳，不用再问一遍） -->
              <el-button
                size="small"
                :disabled="!activeSession.customerNo"
                title="查看客户画像、标签与会话轨迹"
                @click="openCustomer360"
              >
                <el-icon :size="13"><UserFilled /></el-icon>
                客户 360
              </el-button>
            </div>
          </div>
```

**改动 2（第 1514 行附近）** 定位 `* 会话转工单：跳到工单中心并带上会话号与最近一句客户消息。`，在它后面加上：

``` code-block-container
}

/**
 * 客户 360：带着客户编号跳到客户页，直接落在这一位的画像上。
 *
 * <p>为什么不是内嵌抽屉：客户 360 要拉会话轨迹、订单、工单好几块数据，
 * 而且坐席常常是"边看边切回会话"，新开一个页面/标签看更顺手。</p>
 */
function openCustomer360() {
  const customerNo = activeSession.value?.customerNo
  if (!customerNo) {
    ElMessage.warning('这条会话还没有关联客户档案')
    return
  }
  void router.push({ path: '/modules/customers', query: { customerNo } })
}

/**
 * 会话转工单：跳到工单中心并带上会话号与最近一句客户消息。
 *
 * <p>为什么走路由 query 而不是就地弹窗：工单的字段（分类、优先级、处理人、SLA）比较多，
```

### 改动：yunti-frontend/src/views/tickets/index.vue

改动点：支持 `?ticketNo=` 直接打开某张工单（客户 360 的关联工单点一下就跳过来）

这个文件一共 3 处改动，按每处的定位说明直接改（代码块给的都是「改完之后」的样子）：

**改动 1（第 996 行附近）** 定位 `createForm.priority = preset?.priority ?? 2`，把它这一处改成下面这样：

``` code-block-container
  createForm.priority = preset?.priority ?? 2
  createForm.assigneeId = undefined
  createForm.sessionNo = preset?.sessionNo
  // 客户名：会话转单会由后端带上；从客户 360「新建工单」过来时前端预填
  createForm.customerName = preset?.customerName ?? ''
  createForm.sourceChannel = preset?.sourceChannel ?? 1
  createForm.platform = preset?.platform ?? ticketTypeTab.value === 2
  createOpen.value = true
}
```

**改动 2（第 1223 行附近）** 定位 `/** 从工作台"会话转工单"时带着会话号进来（query: sessionNo=...） */`，把它这一处改成下面这样：

``` code-block-container
/** 从工作台"会话转工单"时带着会话号进来（query: sessionNo=...） */
function openCreateFromQuery() {
  const sessionNo = String(router.currentRoute.value.query.sessionNo ?? '').trim()
  // 客户 360 的「新建工单」：只带客户名和主题过来（没有来源会话）
  const customerName = String(router.currentRoute.value.query.customerName ?? '').trim()
  if (!sessionNo && !customerName) {
    return
  }
  // 只读角色（质检专员 / AI运营）从工作台点"转工单"过来：直接说清楚，别让人填完表单才报错
```

**改动 3（第 1234 行附近）** 定位 `openCreate({ sessionNo, title: '会话转工单', content: '' })`，把它这一处改成下面这样：

``` code-block-container
    void router.replace({ path: '/modules/tickets' })
    return
  }
  if (sessionNo) {
    openCreate({ sessionNo, title: '会话转工单', content: '' })
  } else {
    const title = String(router.currentRoute.value.query.title ?? '').trim()
    openCreate({ customerName, title: title || `${customerName} 的咨询`, content: '' })
  }
  void router.replace({ path: '/modules/tickets' })
}

/** 从客户 360 的「关联工单」点进来（query: ticketNo=...）：直接打开那张单的详情 */
function openDetailFromQuery() {
  const ticketNo = String(router.currentRoute.value.query.ticketNo ?? '').trim()
  if (!ticketNo) {
    return
  }
  void openDetail(ticketNo)
  void router.replace({ path: '/modules/tickets' })
}

onMounted(() => {
  openCreateFromQuery()
  openDetailFromQuery()
})

defineExpose({ openCreate })
```

### 改动：yunti-frontend/src/views/visitor/index.vue

改动点：会话结束后出现满意度评价（1\~5 星 + 可选反馈），带访客令牌提交

这个文件一共 5 处改动，按每处的定位说明直接改（代码块给的都是「改完之后」的样子）：

**改动 1（第 161 行附近）** 定位 `:placeholder="sessionClosed ? '本次咨询已结束，如需继续请点右下角重新发起' : '请输入你想咨询的问题，Enter 发送'"`，把它这一处改成下面这样：

``` code-block-container
            :placeholder="sessionClosed ? '本次咨询已结束，如需继续请点右下角重新发起' : '请输入你想咨询的问题，Enter 发送'"
            @keydown.enter.exact.prevent="send"
          />
        <div class="vi-actions">
            <div class="vi-left">
              <input
                ref="fileRef"
```

**改动 2（第 195 行附近）** 定位 `</el-button>` 附近，在它后面加上：

``` code-block-container
              发送
            </el-button>
          </div>
          <!-- 会话结束后请客户给个分：满意度是客户 360 画像里的"口碑"来源 -->
          <div v-if="sessionClosed && !csatDone" class="csat-box">
            <div class="csat-title">本次服务还满意吗？</div>
            <el-rate v-model="csatScore" :max="5" show-score score-template="{value} 分" />
            <el-input
              v-model="csatFeedback"
              class="csat-input"
              maxlength="200"
              show-word-limit
              placeholder="补充一句（可选）"
            />
            <el-button type="primary" size="small" :loading="csatSending" :disabled="!csatScore" @click="submitCsat">
              提交评价
            </el-button>
          </div>
          <div v-else-if="sessionClosed && csatDone" class="csat-box done">
            感谢评价！你的评分已记录，我们会持续改进。
          </div>
        </div>
      </template>
    </div>
```

**改动 3（第 240 行附近）** 定位 `messageTextOf,`，在它后面加上：

``` code-block-container
  messageTextOf,
  openVisitorSession,
  parseImageContent,
  submitSessionCsat,
  uploadChatImage,
  type ChatImageItem,
  type ChatImageUploadResult,
```

**改动 4（第 291 行附近）** 定位 `/** 这条会话有没有人工客服接手（没接手就别说"客服在线"） */`，在它后面加上：

``` code-block-container
/** 这条会话有没有人工客服接手（没接手就别说"客服在线"） */
const hasAgent = ref(false)

/* ---------------- 满意度评价（会话结束后客户打分） ---------------- */
const csatScore = ref(0)
const csatFeedback = ref('')
const csatSending = ref(false)
/** 已经评过就不再显示表单（一条会话只评一次，重复提交是"改评价"） */
const csatDone = ref(false)

/**
 * 提交满意度评价。
 *
 * <p>带的是**访客令牌**，不是登录态：客户没有账号。服务端会校验令牌里的会话号
 * 与路径上的会话号一致，不然谁都能给别人打差评。</p>
 */
async function submitCsat() {
  if (!csatScore.value) {
    ElMessage.warning('先给个星级再提交')
    return
  }
  csatSending.value = true
  try {
    await submitSessionCsat(sessionNo.value, {
      score: csatScore.value,
      feedback: csatFeedback.value.trim() || undefined,
      visitorToken: visitorToken.value,
    })
    csatDone.value = true
    ElMessage.success('评价已提交，谢谢！')
  } finally {
    csatSending.value = false
  }
}

let client: RealtimeClient | null = null

const connected = computed(() => connectionState.value === 'open')
```

**改动 5（第 1617 行附近）** 定位 `margin-top: 10px;`，在它后面加上：

``` code-block-container
  margin-top: 10px;
}

/* 满意度评价：会话结束后在输入区下方出现，别抢主流程的视觉重量 */
.csat-box {
  margin-top: 10px;
  padding: 10px 12px;
  border-radius: 10px;
  background: #f8fafc;
  border: 1px solid #eef2f7;
  display: flex;
  flex-direction: column;
  gap: 8px;
}

.csat-box.done {
  color: #16a34a;
  font-size: 13px;
  background: #f0fdf4;
  border-color: #dcfce7;
}

.csat-title {
  font-size: 13px;
  font-weight: 600;
  color: #334155;
}

.csat-input {
  width: 100%;
}

.vi-tip {
  flex: 1;
  min-width: 0;
```

### 改动：yunti-frontend/src/views/qa/index.vue

改动点：顺手修掉分页的废弃写法：Element Plus 2.14 起 `small` 要写成 `size="small"`

这个文件一共 1 处改动，按每处的定位说明直接改（代码块给的都是「改完之后」的样子）：

**改动 1（第 208 行附近）** 定位 `:total="filteredTasks.length"`，把它这一处改成下面这样：

``` code-block-container
          :total="filteredTasks.length"
          layout="prev, pager, next"
          background
          size="small"
        />
      </div>
    </el-card>
```

## 八、测试验证

### 8.1 环境与启动

``` code-block-container
# 1) 建表/补列：这一篇有增量脚本，第一次跑必须执行（幂等，可重复执行）
bash scripts/migrate-customer-db.sh

# 2) 起服务：网关(9090) + customer-service(9093) + user-service(9091) + 前端(5173)
cd yunti-backend && mvn -pl yunti-gateway spring-boot:run
cd yunti-backend && mvn -pl yunti-customer-service spring-boot:run
cd yunti-backend && mvn -pl yunti-user-service spring-boot:run
cd yunti-frontend && npm run dev
```

### 8.2 页面里怎么测

**① 客户列表**：`运营管理 → 客户 360`。

  

<img src="https://article-images.zsxq.com/FrwUVG-MgTuJhm6QcUrfjfASBeck" class="tiptap-image" alt="图片.png" />

顶部 5 张卡；表格有会员等级、客户类型、标签、互动（会话/转人工/工单）、满意度、最近一次会话；筛「客户类型=企业客户」只剩 B 端客户；

翻到第 2 页应显示第 11–20 位。

  

<img src="https://article-images.zsxq.com/FrwUVG-MgTuJhm6QcUrfjfASBeck" class="tiptap-image" alt="图片.png" />

**② 画像与轨迹**：点行尾「客户 360」→ 画像看数字条与关联工单；

  

<img src="https://article-images.zsxq.com/FszONWfDp7ZIRcaQkrlC01hI6pcu" class="tiptap-image" alt="图片.png" />

切「会话轨迹」看这个人每次会话的接待方式、意图、情绪、满意度，点「聊天记录」看那次的消息。

**③ 打标**：抽屉「标签」页签选一个标签 → 打标 → 客户动态立刻多一条「打标」；列表里的标签、顶部「已打标签」、标签体系页的「已打 N 位客户」同时变。

  

<img src="https://article-images.zsxq.com/Frtzq07KmgGkCtrfJluIqAB6hTpJ" class="tiptap-image" alt="图片.png" />

**④ 规则标签自动跑**：标签体系里把「投诉倾向」的阈值从 2 改成 1 → 保存 →「重算全部客户」→ 回到客户 360，被打上这个标签的客户变多；再改回 2 重算，标签会自动摘掉。（会话结束、建单时引擎也会自己跑一次，不用手点。）

  

<img src="https://article-images.zsxq.com/Fqd3F67LRIBRQWTCwOTiJoMX2bUY" class="tiptap-image" alt="图片.png" />

**⑤ 权限**：用 `scripts/seed-test-accounts.py` 建的账号切换登录——企业管理员能打标/改档案/合并/看完整手机号；客服专员能打标但手机号只有 `138****8888`；质检专员进去只剩"查看"（按钮全部消失）。

  

<img src="https://article-images.zsxq.com/FjpuzniAAhZXBWnNz0AGBRuApVkl" class="tiptap-image" alt="图片.png" />

**⑥ 合规三件套**：抽屉里点「查看完整手机号」→ 客户动态多一条「敏感信息查阅」（带操作人）；「更多 → 匿名化」后姓名变成"已匿名客户"、手机号为空，但会话与统计都在；「更多 → 合并到另一个客户」会把会话/工单/标签/动态迁走，源客户从列表消失。

  

<img src="https://article-images.zsxq.com/FhzMlozQq4HA3m59zGyBzB0fBUsY" class="tiptap-image" alt="图片.png" />

### 小结

### 已完成功能

|                |      |                                                                         |
|----------------|------|-------------------------------------------------------------------------|
| 功能           | 状态 | 说明                                                                    |
| 客户画像       | ✅    | 会话 / 转人工 / 工单 / 满意度 / 意图 / 情绪 / 风险 / 标签，全部系统自算 |
| 标签体系       | ✅    | 定义与关联分离，分组 / 颜色 / 排序 / 启停，可溯源打标人                 |
| 标签自动化     | ✅    | 四件套规则 + 四种触发，命中自动打、不再命中自动摘（只动规则来源）       |
| 会话轨迹       | ✅    | 每次会话一行，能回看聊天记录、能跳去接待                                |
| 客户动态       | ✅    | 16 类事件全留痕，抽屉里按时间线展示                                     |
| 合并与合规     | ✅    | 合并 / 匿名化 / 删除 / 手机号查看（均留痕、均软删）                     |
| 权限收敛       | ✅    | 角色实时反查 user-service；管理动作与手机号查看按角色收敛               |
| 演示数据与自检 | ✅    | 幂等脚本 + 130 项离线断言 + 编译执行的运行时探针                        |

### 技术亮点

1.  **画像只用系统自己算得准的数据**：会话数、转人工次数、工单数、满意度（评价明细的均值）、最近意图与情绪、标签、动态。订单与消费金额虽然看起来"很 CRM"，但客服侧既拿不到实时值也维护不了，摆上去只会让人误以为要人工录。**宁可少一个字段，也不要一个不准的字段**。

2.  **标签"先定义再打标"，规则真能跑**：定义与关联分开，改名/换组/停用只改一处；规则用「指标 + 比较符 + 阈值 + 窗口」表达，指标白名单里只放本系统产生的数据。

3.  **轨迹不另存一份**：轨迹是会话、消息、工单的投影，存两份必然不一致；而"谁对这个客户做过什么"没地方落，才需要 `customer_event`。

4.  **合规动作要留痕、要可回溯**：手机号默认脱敏、查看要权限且写审计；匿名化抹 PII 但保留统计；合并与删除都软删并记下去向。

5.  **框架的"不可变集合"是这一篇最大的暗坑**：`Map.ofEntries` / `Set.of` 连 `getOrDefault(null, …)`、`contains(null)` 都会抛 NPE，而手工标签的字段天然是 null。离线读源码查不出来，所以留了一个**编译并执行**的运行时探针。

6.  **列表页的三件小事，做错了会被当成"功能坏了"**：分页的 `total` 要转 Number、翻页要把页码写回状态、长表格别把分页顶出屏幕。三条都写进了离线断言，避免回归。

### 业务闭环

``` code-block-container
客户进线 ─► 建档 ─► 会话（机器人/人工）─► 转工单 ─► 结束 ─► 评价
                                                      │
              规则标签引擎（会话结束 / 建单 / 定时 / 手动）◄─┘
                      │
                      ▼
   客户 360：画像 + 标签 + 轨迹 + 动态 ─► 坐席接单前先看人 ─► 更准的服务26 客户 360 视图：客户画像、标签体系与会话轨迹
```
