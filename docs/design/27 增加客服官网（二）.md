---
title: "27 增加客服官网（二）"
source: "https://articles.zsxq.com/id_gqsuon1kmpo2.html"
author:
  - "[[苏三]]"
published:
created: 2026-09-25
description:
tags:
  - "clippings"
---
[来自： Java突击队&AI项目实战](https://wx.zsxq.com/group/28851182188851)

### 5.3 步骤11：写交互与数据脚本

新建 `yunti-official/assets/js/site.js`（完整内容）：

``` code-block-container
/**
 * 云梯智能客服 · 官网交互脚本（零依赖，原生 JS）
 *
 * 做五件事：
 *   1. 视觉交互：导航滚动变实、板块进场渐显、FAQ 手风琴、年付/月付切换、订阅提示；
 *   2. **价格同步**：`/api/billing/public/plans` 拉后台真实套餐（与开通下单同一份数据），
 *      失败时用「官网配置里的定价」→ 页面静态兜底，保证离线/爬虫也有价格；
 *   3. **官网配置**：`/api/ops/public/site-config` 覆盖文案（hero / 信任墙 / 功能 / 方案 /
 *      案例 / 数据横幅 / 定价说明 / FAQ / CTA / 页脚），字段契约与平台原型一致；
 *   4. 配置的三种下发方式：`?site=<base64>`（平台预览） > localStorage（已发布） > 后端接口；
 *   5. 入口跳转：登录 / 注册 / 工作台等按钮指向管理端（可用 `?app=` 或
 *      `window.YUNTI_APP_BASE` 覆盖，构建时由 build.mjs 注入）。
 */
(function () {
  'use strict'

  var API_BASE = (document.querySelector('meta[name="yunti-api-base"]') || {}).content || '/api'
  var params = new URLSearchParams(location.search)
  // 管理端地址：默认本地开发端口 5173，部署时用 ?app= / window.YUNTI_APP_BASE 覆盖
  var APP_BASE = params.get('app') || window.YUNTI_APP_BASE || 'http://localhost:5173'
  var CONFIG_KEY = 'yt_site_config_v1'
  var cycle = 'year'
  // 定价卖点前的对勾：与 index.html 静态兜底里的 SVG 完全一致，接口渲染后视觉不跳
  var CHECK_ICON =
    '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M20 6 9 17l-5-5"/></svg>'

  // ---------------------------------------------------------------- 工具
  function $(sel, root) {
    return (root || document).querySelector(sel)
  }

  function $$(sel, root) {
    return Array.prototype.slice.call((root || document).querySelectorAll(sel))
  }

  function get(path, obj) {
    return String(path)
      .split('.')
      .reduce(function (acc, key) {
        return acc == null ? undefined : acc[key]
      }, obj)
  }

  function esc(text) {
    return String(text == null ? '' : text).replace(/[&<>"']/g, function (ch) {
      return { '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' }[ch]
    })
  }

  function toast(title, text) {
    var wrap = $('#toast-wrap')
    if (!wrap) {
      wrap = document.createElement('div')
      wrap.id = 'toast-wrap'
      wrap.className = 'toast-wrap'
      document.body.appendChild(wrap)
    }
    var el = document.createElement('div')
    el.className = 'toast'
    el.innerHTML = '<b>' + esc(title) + '</b>' + (text ? '<span>' + esc(text) + '</span>' : '')
    wrap.appendChild(el)
    setTimeout(function () {
      el.classList.add('leaving')
      setTimeout(function () {
        el.remove()
      }, 260)
    }, 3200)
  }

  /**
   * 价格展示：数字走千分位、0 显示「免费」；
   * 后端 BigDecimal 有可能按字符串回（"5980.00"），所以纯数字字符串也按数字格式化，
   * 其余字符串（比如「联系我们」）原样展示。
   */
  function money(value) {
    if (value == null || value === '') return '免费'
    var raw = typeof value === 'string' ? value.trim() : value
    if (typeof raw === 'string' && !/^\d+(\.\d+)?$/.test(raw)) return raw
    var num = Number(raw)
    if (!num) return '免费'
    return '¥' + num.toLocaleString('zh-CN', { maximumFractionDigits: 0 })
  }

  function isFree(plan) {
    var value = plan.priceY
    return value == null || value === '' || value === '免费' || Number(value) === 0
  }

  // ---------------------------------------------------------------- 视觉交互
  function initNav() {
    var nav = $('#nav')
    if (!nav) return
    var onScroll = function () {
      nav.classList.toggle('scrolled', window.scrollY > 12)
    }
    onScroll()
    window.addEventListener('scroll', onScroll, { passive: true })
  }

  function initReveal() {
    var items = $$('.reveal')
    if (!('IntersectionObserver' in window)) {
      items.forEach(function (el) {
        el.classList.add('in')
      })
      return
    }
    var observer = new IntersectionObserver(
      function (entries) {
        entries.forEach(function (entry, index) {
          if (!entry.isIntersecting) return
          entry.target.style.transitionDelay = (index % 3) * 0.06 + 's'
          entry.target.classList.add('in')
          observer.unobserve(entry.target)
        })
      },
      { threshold: 0.12 },
    )
    items.forEach(function (el) {
      observer.observe(el)
    })
  }

  /** FAQ 手风琴：同一时间只展开一条 */
  function initFaq() {
    $$('.faq-item').forEach(function (item) {
      var head = $('.faq-q', item)
      if (!head || head.dataset.bound) return
      head.dataset.bound = '1'
      head.addEventListener('click', function () {
        var open = item.classList.contains('open')
        $$('.faq-item').forEach(function (other) {
          other.classList.remove('open')
        })
        item.classList.toggle('open', !open)
      })
    })
  }

  function initCycleToggle() {
    var buttons = $$('.pt-btn')
    if (!buttons.length) return
    buttons.forEach(function (btn) {
      btn.addEventListener('click', function () {
        buttons.forEach(function (other) {
          other.classList.toggle('active', other === btn)
        })
        cycle = btn.dataset.cycle || 'year'
        applyCycle()
      })
    })
  }

  /** 切换年付/月付：只换价格与单位，功能列表不动（年付时按原型展示划线原价） */
  function applyCycle() {
    $$('#price-grid .plan').forEach(function (card) {
      var holder = $('[data-price-y]', card)
      if (!holder) return
      var value = cycle === 'month' ? holder.dataset.priceM : holder.dataset.priceY
      var label = $('[data-price-val]', card)
      var per = $('small', holder)
      if (label && value != null) label.textContent = value
      if (per) per.textContent = cycle === 'month' ? '/月' : '/年'
    })
  }

  function initSubscribe() {
    var form = $('#subscribe-form')
    if (!form) return
    form.addEventListener('submit', function (event) {
      event.preventDefault()
      var email = (form.elements.email.value || '').trim()
      if (!/^[^@\s]+@[^@\s]+\.[^@\s]+$/.test(email)) {
        toast('邮箱格式不正确', '请输入有效的工作邮箱')
        return
      }
      // 官网是静态站点：这里先本地留档并提示，正式订阅名单由运营侧接口承接
      try {
        var list = JSON.parse(localStorage.getItem('yt_site_subscribers_v1') || '[]')
        if (list.indexOf(email) < 0) list.push(email)
        localStorage.setItem('yt_site_subscribers_v1', JSON.stringify(list))
      } catch (e) {
        /* 忽略本地存储异常 */
      }
      toast('订阅成功', '感谢订阅，最新动态将发送至 ' + email)
      form.reset()
    })
  }

  // ---------------------------------------------------------------- 定价（真实套餐）

  /** 从后台套餐字段推导卖点文案（配额以 plan 表为准，官网不写死数字） */
  function planFeatures(plan) {
    var feats = []
    feats.push(numberText(plan.quotaRobotSessions) + ' 次/月机器人会话')
    feats.push(plan.quotaChannels + ' 个渠道 · ' + plan.quotaSeats + ' 个坐席')
    if (plan.quotaAiQa) {
      feats.push('全量 AI 质检' + (plan.quotaVision ? ' + 视觉识别（图片判读）' : ''))
    } else if (plan.quotaAiCalls) {
      feats.push(numberText(plan.quotaAiCalls) + ' 次/月 AI 调用')
    }
    if (plan.overagePricePerK) {
      feats.push('超量按 ¥' + plan.overagePricePerK + '/千次计费')
    } else if (plan.quotaStorageGb) {
      feats.push(plan.quotaStorageGb + ' GB 知识库存储')
    }
    return feats
  }

  function numberText(value) {
    var num = Number(value || 0)
    return num >= 1000 ? num.toLocaleString('zh-CN') : String(num)
  }

  function fromApi(plan) {
    return {
      code: plan.planCode,
      name: plan.planName,
      desc: plan.planCode === 'FREE' ? '注册即试用，额度用完再升级' : plan.planName + ' · 按需升级',
      priceY: plan.priceYearly,
      priceM: plan.priceMonthly,
      feats: planFeatures(plan),
      cta: plan.planCode === 'FREE' ? '免费开始' : plan.planCode === 'ENTERPRISE' ? '联系我们' : '立即开通',
      hot: plan.planCode === 'PROFESSIONAL',
    }
  }

  function fromConfig(plan, index) {
    return {
      code: plan.code || 'CFG_' + index,
      name: plan.name,
      desc: plan.desc || '',
      priceY: plan.priceY,
      priceM: plan.priceM,
      feats: plan.feats || [],
      cta: plan.cta || '立即开通',
      hot: !!plan.hot,
    }
  }

  function renderPlans(plans) {
    var grid = $('#price-grid')
    if (!grid || !plans.length) return
    grid.innerHTML = plans
      .map(function (plan) {
        var free = isFree(plan)
        var priceHtml = free
          ? '<div class="price free"><span data-price-val>' + esc(money(plan.priceY)) + '</span></div>'
          : '<div class="price" data-price-y="' +
            esc(money(plan.priceY)) +
            '" data-price-m="' +
            esc(money(plan.priceM)) +
            '"><span data-price-val>' +
            esc(money(plan.priceY)) +
            '</span><small>/年</small></div>'
        return (
          '<div class="plan' +
          (plan.hot ? ' hot' : '') +
          '" data-plan-code="' +
          esc(plan.code) +
          '">' +
          (plan.hot ? '<div class="rib">最受欢迎</div>' : '') +
          '<h4>' +
          esc(plan.name) +
          '</h4>' +
          '<div class="desc">' +
          esc(plan.desc) +
          '</div>' +
          priceHtml +
          '<ul>' +
          plan.feats
            .map(function (text) {
              return '<li>' + CHECK_ICON + esc(text) + '</li>'
            })
            .join('') +
          '</ul>' +
          '<a class="btn ' +
          (plan.hot ? 'btn-primary' : 'btn-outline') +
          ' btn-block" href="#" data-app-link="/register">' +
          esc(plan.cta) +
          '</a>' +
          '</div>'
        )
      })
      .join('')
    bindAppLinks()
    applyCycle()
  }

  /**
   * 定价数据源优先级：后台套餐接口（真实价格） > 官网配置里的定价 > 页面静态兜底。
   * configPromise 与请求并行，避免串行等待。
   */
  function syncPlans(configPromise) {
    fetch(API_BASE + '/billing/public/plans', { headers: { Accept: 'application/json' } })
      .then(function (res) {
        return res.ok ? res.json() : null
      })
      .then(function (body) {
        if (body && body.code === 0 && Array.isArray(body.data) && body.data.length) {
          renderPlans(body.data.map(fromApi))
          return
        }
        throw new Error('empty plans')
      })
      .catch(function () {
        // 后台没起 / 静态部署：用配置里的定价，再不行保留页面静态兜底
        configPromise.then(function (cfg) {
          var plans = cfg && cfg.pricing && cfg.pricing.plans
          if (Array.isArray(plans) && plans.length) renderPlans(plans.map(fromConfig))
        })
      })
  }

  // ---------------------------------------------------------------- 官网配置

  function decodeSiteParam(raw) {
    try {
      var b64 = String(raw).replace(/-/g, '+').replace(/_/g, '/')
      return JSON.parse(decodeURIComponent(escape(atob(b64))))
    } catch (e) {
      return null
    }
  }

  function applySiteConfig(cfg) {
    if (!cfg || typeof cfg !== 'object') return

    // 1) 标量文案：hero / 功能 / 方案 / 案例 / 数据横幅 / 定价说明 / CTA / 页脚
    $$('[data-cfg]').forEach(function (el) {
      var value = get(el.dataset.cfg, cfg)
      if (value != null && value !== '' && typeof value !== 'object') el.textContent = value
    })

    // 2) 信任 Logo 墙：逗号分隔字符串或数组都能吃
    var trust = cfg.trust || {}
    if (trust.brands) {
      var brands = Array.isArray(trust.brands) ? trust.brands : String(trust.brands).split(/[,，]/)
      var row = $('#trust-brands')
      if (row) {
        row.innerHTML = brands
          .map(function (name) {
            return String(name).trim()
          })
          .filter(Boolean)
          .map(function (name) {
            return '<span>' + esc(name) + '</span>'
          })
          .join('')
      }
    }

    // 3) Hero 统计：兼容 [{v,l}] 与 [{value,label}] 两种字段名
    var stats = (cfg.hero && cfg.hero.stats) || cfg.stats
    if (Array.isArray(stats) && stats.length) {
      var box = $('#hero-stats')
      if (box) {
        box.innerHTML = stats
          .map(function (item) {
            var value = item.v != null ? item.v : item.value
            var label = item.l != null ? item.l : item.label
            return '<div><b>' + esc(value) + '</b><span>' + esc(label) + '</span></div>'
          })
          .join('')
      }
    }

    // 4) FAQ：条数可变，重建后重新绑定手风琴
    var faq = cfg.faq || {}
    if (Array.isArray(faq.items) && faq.items.length) {
      var list = $('#faq-list')
      if (list) {
        list.innerHTML = faq.items
          .map(function (item) {
            return (
              '<div class="faq-item"><div class="faq-q">' +
              esc(item.q) +
              '<span class="pm">＋</span></div><div class="faq-a">' +
              esc(item.a) +
              '</div></div>'
            )
          })
          .join('')
        initFaq()
      }
    }
  }

  /** 拉官网配置：返回 Promise<配置对象|null>，失败静默（页面用默认文案） */
  function syncSiteConfig() {
    var fromQuery = params.get('site') ? decodeSiteParam(params.get('site')) : null
    if (fromQuery) {
      applySiteConfig(fromQuery)
      return Promise.resolve(fromQuery)
    }
    try {
      var cached = localStorage.getItem(CONFIG_KEY)
      if (cached) {
        var parsed = JSON.parse(cached)
        applySiteConfig(parsed)
        return Promise.resolve(parsed)
      }
    } catch (e) {
      /* 缓存坏了就继续走接口 */
    }
    return fetch(API_BASE + '/ops/public/site-config', { headers: { Accept: 'application/json' } })
      .then(function (res) {
        return res.ok ? res.json() : null
      })
      .then(function (body) {
        if (body && body.code === 0 && body.data) {
          applySiteConfig(body.data)
          return body.data
        }
        return null
      })
      .catch(function () {
        return null
      })
  }

  // ---------------------------------------------------------------- 入口链接
  function bindAppLinks() {
    $$('[data-app-link]').forEach(function (el) {
      var path = el.dataset.appLink
      // 部署时如果已经把 href 写成生产绝对地址，就不再覆盖
      if (/^https?:/.test(el.getAttribute('href') || '')) return
      el.setAttribute('href', APP_BASE.replace(/\/$/, '') + path)
    })
  }

  // ---------------------------------------------------------------- 启动
  document.addEventListener('DOMContentLoaded', function () {
    initNav()
    initReveal()
    initFaq()
    initCycleToggle()
    initSubscribe()
    bindAppLinks()
    var configPromise = syncSiteConfig()
    syncPlans(configPromise)
  })
})()
```

这个脚本干了五件事，每件都对应一个真实场景：

<table style="min-width;"><colgroup><col style="width: 50%" /><col style="width: 50%" /></colgroup><tbody><tr class="header"><th class="tiptap-table-header"><p>功能</p></th><th class="tiptap-table-header"><p>说明</p></th></tr><tr class="odd"><td class="tiptap-table-cell"><p>视觉交互</p></td><td class="tiptap-table-cell"><p>滚动时导航变实、板块渐显、FAQ 手风琴、年付/月付切换</p></td></tr><tr class="even"><td class="tiptap-table-cell"><p><strong>价格同步</strong></p></td><td class="tiptap-table-cell"><p>拉 <code>/api/billing/public/plans</code>，用真实套餐重画定价卡片</p></td></tr><tr class="odd"><td class="tiptap-table-cell"><p><strong>文案下发</strong></p></td><td class="tiptap-table-cell"><p>拉 <code>/api/ops/public/site-config</code>，按 <code>data-cfg</code> 覆盖文案</p></td></tr><tr class="even"><td class="tiptap-table-cell"><p>失败降级</p></td><td class="tiptap-table-cell"><p>接口挂了就保留静态兜底（页面里的默认价与默认文案）</p></td></tr><tr class="odd"><td class="tiptap-table-cell"><p>入口跳转</p></td><td class="tiptap-table-cell"><p>「登录 / 免费试用」跳管理端（默认 5173，可用 <code>?app=</code> 覆盖）</p></td></tr><tr class="even"><td class="tiptap-table-cell"><p>官网配置的优先级（从高到低）是：<strong>URL 参数</strong> <code>?site=base64</code> <strong>&gt; 浏览器缓存 &gt; 后端接口</strong>，这样平台侧「官网配置」页做预览、发布时都不用改官网代码。</p></td><td class="tiptap-table-cell"><p><br />
</p></td></tr></tbody></table>

## 产品实时演示页

### 6.1 步骤12：写演示页

新建 `yunti-official/demo.html`（完整内容）。它模拟一个「客户自己的网站」，右下角挂着访客端客服组件，点开就能体验：机器人接待 → 知识库引用 → 转人工 → 满意度评价：

``` code-block-container
<!doctype html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>产品实时演示 · 云梯智能客服</title>
  <meta name="robots" content="noindex" />
  <link rel="stylesheet" href="assets/css/site.css" />
  <style>
    /* 演示页：一个「客户自己的网站」+ 右下角挂载的客服组件，用来展示访客看到的效果 */
    html, body { min-height: 100%; }
    body { background: #f6f7f9; min-height: 100vh; display: flex; flex-direction: column; }
    .demo-nav {
      display: flex; align-items: center; gap: 24px;
      height: 58px; padding: 0 32px; background: rgba(255, 255, 255, 0.92);
      border-bottom: 1px solid var(--line); backdrop-filter: blur(10px);
    }
    .demo-nav .brand { display: flex; align-items: center; gap: 9px; font-weight: 700; font-size: 15.5px; white-space: nowrap; }
    .demo-nav .mark { width: 28px; height: 28px; border-radius: 9px; background: linear-gradient(135deg, #3b82f6, #0ea5e9); color: #fff; display: flex; align-items: center; justify-content: center; flex-shrink: 0; }
    .demo-nav nav { display: flex; gap: 22px; font-size: 13.5px; color: var(--ink-2); }
    .demo-nav nav a:hover { color: var(--brand); }
    .demo-nav .tip { margin-left: auto; font-size: 12.5px; color: var(--ink-3); }

    .demo-hero {
      max-width: 1080px; margin: 26px auto 0; padding: 34px 40px;
      background: linear-gradient(120deg, #eff6ff 0%, #f0f9ff 55%, #ecfeff 100%);
      border: 1px solid var(--line); border-radius: 18px;
      display: flex; align-items: center; justify-content: space-between; gap: 32px;
    }
    .demo-hero h1 { font-size: 28px; letter-spacing: -0.6px; }
    .demo-hero p { color: var(--ink-2); margin-top: 10px; max-width: 470px; font-size: 14px; }
    .demo-hero .hero-badge { display: inline-flex; align-items: center; gap: 6px; background: #fff; color: var(--brand); padding: 5px 12px; border-radius: 999px; font-size: 12px; font-weight: 600; border: 1px solid var(--line); }

    .demo-grid { max-width: 1080px; margin: 22px auto 40px; padding: 0 40px; display: grid; grid-template-columns: repeat(4, 1fr); gap: 16px; }
    .p-card { background: #fff; border: 1px solid var(--line); border-radius: 14px; overflow: hidden; }
    .p-card .p-img { height: 140px; display: flex; align-items: center; justify-content: center; }
    .p-card .p-img svg { width: 52px; height: 52px; color: #fff; opacity: .92; }
    .p-card .p-body { padding: 13px 15px; }
    .p-card .p-name { font-weight: 600; font-size: 14px; }
    .p-card .p-price { color: #e11d48; font-weight: 700; margin-top: 5px; font-size: 15px; }

    /* ── 客服组件（访客端） ─────────────────────────────── */
    .fab {
      position: fixed; right: 28px; bottom: 28px; z-index: 40;
      width: 56px; height: 56px; border-radius: 50%; border: none; cursor: pointer;
      background: linear-gradient(135deg, #2563eb, #0ea5e9); color: #fff;
      display: flex; align-items: center; justify-content: center;
      box-shadow: 0 14px 30px -8px rgba(37, 99, 235, 0.6); transition: transform 0.2s ease;
    }
    .fab:hover { transform: scale(1.06); }
    .fab svg { width: 24px; height: 24px; }
    .fab .badge-num {
      position: absolute; top: -4px; right: -4px; min-width: 20px; height: 20px; padding: 0 5px;
      border-radius: 999px; background: #ef4444; color: #fff; font-size: 11.5px; font-weight: 700;
      display: flex; align-items: center; justify-content: center; border: 2px solid #fff;
    }
    .fab .badge-num[hidden] { display: none; }

    .chatbox {
      position: fixed; right: 26px; bottom: 98px; z-index: 50;
      width: 386px; max-width: calc(100vw - 32px); height: 556px; max-height: calc(100vh - 130px);
      background: #fff; border-radius: 16px; box-shadow: 0 30px 70px -20px rgba(13, 16, 32, 0.35);
      display: flex; flex-direction: column; overflow: hidden;
      transform: translateY(16px) scale(0.97); opacity: 0; pointer-events: none;
      transition: all 0.25s cubic-bezier(0.32, 0.72, 0, 1);
    }
    .chatbox.open { transform: none; opacity: 1; pointer-events: auto; }
    .cb-head { display: flex; align-items: center; gap: 12px; padding: 14px 16px; color: #fff; background: linear-gradient(120deg, #2563eb, #0ea5e9); }
    .cb-head .cb-av { width: 36px; height: 36px; border-radius: 50%; background: rgba(255, 255, 255, 0.18); display: flex; align-items: center; justify-content: center; font-weight: 700; font-size: 13px; flex-shrink: 0; }
    .cb-head .cb-name { font-weight: 700; font-size: 14.5px; }
    .cb-head .cb-sub { font-size: 11.5px; opacity: 0.85; display: flex; align-items: center; gap: 5px; margin-top: 2px; }
    .cb-head .cb-sub .dot { width: 7px; height: 7px; border-radius: 50%; background: #6ee7b7; }
    .cb-head .actions { margin-left: auto; display: flex; gap: 4px; }
    .cb-head .icon-btn { background: none; border: none; color: #fff; cursor: pointer; width: 28px; height: 28px; border-radius: 8px; display: flex; align-items: center; justify-content: center; }
    .cb-head .icon-btn:hover { background: rgba(255, 255, 255, 0.16); }
    .cb-head .icon-btn svg { width: 16px; height: 16px; }

    .cb-body { flex: 1; overflow-y: auto; padding: 16px; background: #fbfcfe; }
    .cb-greet { background: var(--brand-soft); border-radius: 12px; padding: 12px 14px; font-size: 12.5px; color: var(--ink-2); margin-bottom: 14px; line-height: 1.7; }
    .msg { display: flex; gap: 9px; margin-bottom: 12px; align-items: flex-start; }
    .msg .av { width: 30px; height: 30px; border-radius: 50%; display: flex; align-items: center; justify-content: center; font-size: 11.5px; font-weight: 700; color: #fff; flex-shrink: 0; }
    .msg .bubble { background: #fff; border: 1px solid var(--line); border-radius: 12px; border-top-left-radius: 4px; padding: 9px 13px; font-size: 13px; line-height: 1.7; color: var(--ink); max-width: 260px; word-break: break-word; }
    .msg.customer { flex-direction: row-reverse; }
    .msg.agent .bubble { background: #fff; }
    .msg.customer .bubble { background: linear-gradient(120deg, #2563eb, #0ea5e9); color: #fff; border: none; border-radius: 12px; border-top-right-radius: 4px; }
    .msg .meta { font-size: 11px; color: var(--ink-3); margin-top: 5px; }
    .msg .suggest { margin-top: 8px; background: #fff; border: 1px solid var(--line); border-left: 3px solid var(--brand); border-radius: 10px; padding: 10px 12px; font-size: 12px; color: var(--ink-2); max-width: 300px; }
    .msg .suggest .s-title { color: var(--brand); font-weight: 600; margin-bottom: 4px; }
    .msg .suggest button { margin-top: 8px; height: 28px; padding: 0 12px; font-size: 12px; border-radius: 8px; border: 1px solid var(--line); background: #fff; color: var(--brand); cursor: pointer; }
    .msg .suggest button:hover { background: var(--brand-soft); }
    .sysline { text-align: center; font-size: 11.5px; color: var(--ink-3); margin: 14px 0; }
    .typing { display: inline-flex; gap: 4px; align-items: center; height: 16px; }
    .typing span { width: 6px; height: 6px; border-radius: 50%; background: #b6bac8; animation: blink 1.2s infinite; }
    .typing span:nth-child(2) { animation-delay: 0.2s; }
    .typing span:nth-child(3) { animation-delay: 0.4s; }
    @keyframes blink { 0%, 60%, 100% { opacity: 0.35; } 30% { opacity: 1; } }

    .quickq { display: flex; flex-wrap: wrap; gap: 7px; margin: 4px 0 14px; }
    .quickq button { border: 1px solid var(--line); background: #fff; border-radius: 999px; padding: 7px 13px; font-size: 12px; color: var(--ink-2); cursor: pointer; transition: all 0.18s ease; font-family: inherit; }
    .quickq button:hover { border-color: var(--brand); color: var(--brand); background: var(--brand-soft); }
    .csat { background: #fff; border: 1px solid var(--line); border-radius: 12px; padding: 13px; text-align: center; margin: 0 0 14px 39px; font-size: 12.5px; color: var(--ink-2); }
    .csat .stars { display: flex; gap: 6px; justify-content: center; margin-top: 8px; font-size: 20px; }
    .csat .stars button { background: none; border: none; cursor: pointer; color: #d4d8e0; font-size: 20px; padding: 0; line-height: 1; }
    .csat .stars button.on { color: #f59e0b; }

    .cb-input { border-top: 1px solid var(--line); padding: 11px 13px; display: flex; gap: 9px; align-items: flex-end; background: #fff; }
    .cb-input textarea { flex: 1; min-width: 0; border: none; resize: none; font-size: 13px; max-height: 76px; font-family: inherit; outline: none; line-height: 1.6; }
    .cb-input .send { width: 36px; height: 36px; border-radius: 10px; border: none; cursor: pointer; background: linear-gradient(135deg, #2563eb, #0ea5e9); color: #fff; display: flex; align-items: center; justify-content: center; flex-shrink: 0; }
    .cb-input .send svg { width: 17px; height: 17px; }

    .demo-strip { max-width: 1080px; margin: 6px auto 0; padding: 0 40px; display: grid; grid-template-columns: repeat(4, 1fr); gap: 16px; }
    .demo-strip .st { background: #fff; border: 1px solid var(--line); border-radius: 14px; padding: 18px 20px; }
    .demo-strip .st .si { width: 36px; height: 36px; border-radius: 11px; display: flex; align-items: center; justify-content: center; color: #fff; margin-bottom: 12px; }
    .demo-strip .st .si svg { width: 18px; height: 18px; }
    .demo-strip .st b { font-size: 14px; }
    .demo-strip .st p { color: var(--ink-3); font-size: 12.5px; margin-top: 6px; line-height: 1.7; }
    .demo-foot { max-width: 1080px; margin: auto auto 0; padding: 34px 40px 40px; font-size: 12.5px; color: var(--ink-3); line-height: 1.9; }
    .toast-wrap { position: fixed; right: 22px; top: 22px; z-index: 200; display: grid; gap: 10px; }
    .toast { background: #0f172a; color: #fff; border-radius: 12px; padding: 12px 18px; box-shadow: 0 18px 44px -18px rgba(13, 16, 32, 0.5); font-size: 13px; animation: fadeIn 0.24s ease; }

    @media (max-width: 860px) {
      .demo-grid { grid-template-columns: repeat(2, 1fr); padding: 0 20px; }
      .demo-hero { margin: 18px 20px 0; padding: 24px; }
      .demo-nav nav { display: none; }
      .demo-strip { grid-template-columns: repeat(2, 1fr); padding: 0 20px; }
      .demo-foot { padding: 28px 20px 32px; }
    }
  </style>
</head>
<body>

<!-- 客户自己的网站（演示载体） -->
<header class="demo-nav">
  <div class="brand"><span class="mark">云</span>云梯商城</div>
  <nav><a href="#top">首页</a><a href="#top">热卖</a><a href="#top">以旧换新</a><a href="#top">企业采购</a></nav>
  <span class="tip">↓ 右下角就是接入的客服组件（点它试试）</span>
</header>
<section class="demo-hero" id="top">
  <div>
    <span class="hero-badge">🎧 7×24 智能客服在线</span>
    <h1>云雀智能家居旗舰店</h1>
    <p>物流、退换、发票、会员积分都可以直接问右下角的智能客服「小云」，答不上来会转真人客服。</p>
  </div>
</section>
<div class="demo-grid">
  <div class="p-card">
    <div class="p-img" style="background:linear-gradient(135deg,#3b82f6,#0ea5e9)"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"><rect x="5" y="2" width="14" height="20" rx="3"/><path d="M9 6h6"/><circle cx="12" cy="17" r="1.4"/></svg></div>
    <div class="p-body"><div class="p-name">「云雀」智能音箱 Pro</div><div class="p-price">¥ 699</div></div>
  </div>
  <div class="p-card">
    <div class="p-img" style="background:linear-gradient(135deg,#10b981,#34d399)"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="4" width="18" height="14" rx="2"/><path d="M8 21h8"/><path d="M12 18v3"/></svg></div>
    <div class="p-body"><div class="p-name">智能中控屏 8 英寸</div><div class="p-price">¥ 1,299</div></div>
  </div>
  <div class="p-card">
    <div class="p-img" style="background:linear-gradient(135deg,#f59e0b,#ea580c)"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"><path d="M4 4h16v12H4z"/><path d="M2 20h20"/><path d="M9 8h6"/></svg></div>
    <div class="p-body"><div class="p-name">全自动咖啡机 · 联名款</div><div class="p-price">¥ 2,899</div></div>
  </div>
  <div class="p-card">
    <div class="p-img" style="background:linear-gradient(135deg,#ec4899,#db2777)"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"><path d="M6 2 3 6v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2V6l-3-4z"/><path d="M3 6h18"/><path d="M16 10a4 4 0 0 1-8 0"/></svg></div>
    <div class="p-body"><div class="p-name">入户安装服务包</div><div class="p-price">¥ 199</div></div>
  </div>
</div>
<div class="demo-strip">
  <div class="st">
    <span class="si" style="background:linear-gradient(135deg,#3b82f6,#0ea5e9)"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="9"/><path d="M3 12h18M12 3a13.5 13.5 0 0 1 0 18M12 3a13.5 13.5 0 0 0 0 18"/></svg></span>
    <b>六大渠道接入</b>
    <p>网站、微信、小程序、App、400 热线、邮件，一处配置全端生效。</p>
  </div>
  <div class="st">
    <span class="si" style="background:linear-gradient(135deg,#10b981,#059669)"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M4 19.5A2.5 2.5 0 0 1 6.5 17H20V3H6.5A2.5 2.5 0 0 0 4 5.5z"/><path d="M4 19.5A2.5 2.5 0 0 0 6.5 22H20v-5"/></svg></span>
    <b>知识库驱动</b>
    <p>回答带引用可溯源，答不上来诚实兜底，绝不编答案。</p>
  </div>
  <div class="st">
    <span class="si" style="background:linear-gradient(135deg,#f59e0b,#ea580c)"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M12 22s8-3.5 8-10V5l-8-3-8 3v7c0 6.5 8 10 8 10z"/><path d="m8.5 12 2.5 2.5 4.5-5"/></svg></span>
    <b>实时质检</b>
    <p>违规话术秒级预警，人工接管后全程留痕可复盘。</p>
  </div>
  <div class="st">
    <span class="si" style="background:linear-gradient(135deg,#06b6d4,#0891b2)"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M3 3v18h18"/><rect x="7" y="12" width="3" height="6" rx="1"/><rect x="12" y="8" width="3" height="10" rx="1"/><rect x="17" y="5" width="3" height="13" rx="1"/></svg></span>
    <b>数据可下钻</b>
    <p>会话、坐席、满意度与高频问题实时统计。</p>
  </div>
</div>
<div class="demo-foot">
  本页是一个演示用的「客户网站」，右下角挂载的是云梯访客端客服组件：机器人先接待，
  命中知识库时给出引用来源，回复「转人工」会进入人工接待并触发满意度评价。
  正式接入时，你在控制台「渠道接入」里复制一段 SDK 代码贴到自己网站上即可，效果完全一致。
</div>
<!-- ── 访客端客服组件 ─────────────────────────────────── -->
<button class="fab" id="fab" type="button" aria-label="打开在线客服">
  <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M21 15a2 2 0 0 1-2 2H8l-5 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"/></svg>
  <span class="badge-num" id="fab-badge" hidden>1</span>
</button>
<div class="chatbox" id="chatbox" role="dialog" aria-label="在线客服">
  <div class="cb-head">
    <div class="cb-av" id="agent-av">云</div>
    <div>
      <div class="cb-name" id="agent-name">小云智能客服</div>
      <div class="cb-sub"><span class="dot"></span><span id="agent-status">在线 · 通常 10 秒内回复</span></div>
    </div>
    <div class="actions">
      <button class="icon-btn" id="btn-min" type="button" aria-label="最小化"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14"/></svg></button>
      <button class="icon-btn" id="btn-close" type="button" aria-label="关闭"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="M18 6 6 18M6 6l12 12"/></svg></button>
    </div>
  </div>
  <div class="cb-body" id="cb-body">
    <div class="cb-greet">
      您好，我是智能客服 <b>小云</b>，很高兴为您服务～<br />
      物流、退换货、发票、会员积分这类问题我都能查，需要人工时回复「转人工」即可。
    </div>
    <div class="msg agent">
      <div class="av" style="background:linear-gradient(135deg,#3b82f6,#0ea5e9)">云</div>
      <div><div class="bubble">请问有什么可以帮您？</div></div>
    </div>
    <div class="quickq" id="quickq">
      <button type="button" data-q="快递到哪了？">查物流</button>
      <button type="button" data-q="怎么退货？">退货规则</button>
      <button type="button" data-q="发票抬头怎么改？">发票问题</button>
      <button type="button" data-q="会员积分怎么算？">会员积分</button>
      <button type="button" data-q="转人工">转人工</button>
    </div>
  </div>
  <div class="cb-input">
    <textarea id="cb-text" rows="1" placeholder="请输入您的问题，Enter 发送…" aria-label="输入消息"></textarea>
    <button class="send" id="btn-send" type="button" aria-label="发送">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><path d="m22 2-7 20-4-9-9-4z"/><path d="M22 2 11 13"/></svg>
    </button>
  </div>
</div>
<script>
  /* 演示用知识库：正式环境由「AI 客服大脑 + 企业知识库」实时生成，这里用固定问答还原交互效果 */
  var KB = [
    { k: ['物流', '快递', '到哪', '发货', '配送'], a: '已为您查询到订单正在转运途中，预计今日 18:00 前送达。因中转天气延误，已自动发放 5 元延迟关怀券，可在【我的-优惠券】查看～', kb: '物流异常自动关怀与补偿标准（SOP）' },
    { k: ['退', '换', '运费'], a: '7 天无理由退货：未拆封商品运费由买家承担；质量问题退货由商家承担，我们承担上门取件费用。需要我帮您发起退货申请吗？', kb: '7 天无理由退货的运费承担规则' },
    { k: ['发票', '抬头', '开票'], a: '电子发票支持自助修改抬头：订单页 → 申请开票 → 修改抬头，1 个工作日内重新开具并推送到您的邮箱～', kb: '发票抬头变更与重开申请流程' },
    { k: ['积分', '会员'], a: '会员积分有效期为 24 个月，会员日双倍累积；联名款咖啡机需 128,000 积分兑换。需要我帮您查询当前积分明细吗？', kb: '铂金会员积分有效期与兑换规则' },
    { k: ['蓝牙', '连接', '断连', '音箱'], a: '「云雀」智能音箱 Pro 支持蓝牙 5.3；若出现断连，建议升级固件到 v2.4 以上并重置网络配置，需要我发送图文操作指引吗？', kb: '「云雀」智能音箱蓝牙连接常见问题排查' },
    { k: ['优惠', '券', '折扣'], a: '优惠券使用失败通常是三种原因：已过期 / 未达门槛 / 品类限制。把券码发我，我帮您核实并申请补发～', kb: '优惠券使用失败常见原因' }
  ]
  var HUMAN = { k: ['人工', '客服', '投诉'], a: '好的，正在为您转接人工客服，客服将在 30 秒内接入，请稍等～' }

  var $ = function (id) { return document.getElementById(id) }
  var body = $('cb-body')
  var input = $('cb-text')
  var handover = false

  function now() {
    var d = new Date()
    var p = function (n) { return (n < 10 ? '0' : '') + n }
    return p(d.getHours()) + ':' + p(d.getMinutes())
  }

  function scrollBottom() { body.scrollTop = body.scrollHeight }

  function el(tag, cls, text) {
    var node = document.createElement(tag)
    if (cls) node.className = cls
    if (text != null) node.textContent = text
    return node
  }

  function agentMsg(text) {
    var wrap = el('div', 'msg agent')
    var av = el('div', 'av', handover ? '陈' : '云')
    av.style.background = handover ? 'linear-gradient(135deg,#10b981,#059669)' : 'linear-gradient(135deg,#3b82f6,#0ea5e9)'
    var col = el('div')
    col.appendChild(el('div', 'bubble', text))
    col.appendChild(el('div', 'meta', (handover ? '陈思远（人工）' : '小云（智能客服）') + ' · ' + now()))
    wrap.appendChild(av)
    wrap.appendChild(col)
    body.appendChild(wrap)
    scrollBottom()
    return wrap
  }

  function customerMsg(text) {
    var wrap = el('div', 'msg customer')
    var av = el('div', 'av', '我')
    av.style.background = 'linear-gradient(135deg,#94a3b8,#64748b)'
    var col = el('div')
    col.appendChild(el('div', 'bubble', text))
    col.appendChild(el('div', 'meta', now()))
    wrap.appendChild(av)
    wrap.appendChild(col)
    body.appendChild(wrap)
    scrollBottom()
  }

  function sysline(text) {
    body.appendChild(el('div', 'sysline', text))
    scrollBottom()
  }

  function knowledge(value) {
    var card = el('div', 'suggest')
    card.appendChild(el('div', 's-title', '📚 知识推荐 · ' + value))
    card.appendChild(el('div', null, '以上回复由企业知识库检索生成，点开可查看原文。'))
    var btn = el('button', null, '查看原文')
    btn.type = 'button'
    btn.addEventListener('click', function () { flash('已打开知识原文', '正式环境会在新窗口打开知识库文档') })
    card.appendChild(btn)
    return card
  }

  function csat() {
    var box = el('div', 'csat', '请为本次服务评分：')
    var stars = el('div', 'stars')
    ;[1, 2, 3, 4, 5].forEach(function (n) {
      var b = el('button', null, '★')
      b.type = 'button'
      b.addEventListener('click', function () {
        Array.prototype.slice.call(stars.children).forEach(function (s, i) {
          s.classList.toggle('on', i < n)
        })
        flash('感谢您的评价', n >= 4 ? '已记录 5 星好评，谢谢支持' : '我们会针对您反馈的问题持续改进')
      })
      stars.appendChild(b)
    })
    box.appendChild(stars)
    body.appendChild(box)
    scrollBottom()
  }

  function typing() {
    var wrap = el('div', 'msg agent')
    var av = el('div', 'av', handover ? '陈' : '云')
    av.style.background = handover ? 'linear-gradient(135deg,#10b981,#059669)' : 'linear-gradient(135deg,#3b82f6,#0ea5e9)'
    var col = el('div')
    var bubble = el('div', 'bubble')
    var t = el('div', 'typing')
    t.innerHTML = '<span></span><span></span><span></span>'
    bubble.appendChild(t)
    col.appendChild(bubble)
    wrap.appendChild(av)
    wrap.appendChild(col)
    body.appendChild(wrap)
    scrollBottom()
    return wrap
  }

  function flash(title, text) {
    var wrap = document.querySelector('.toast-wrap')
    if (!wrap) {
      wrap = el('div', 'toast-wrap')
      document.body.appendChild(wrap)
    }
    var t = el('div', 'toast')
    var b = el('b', null, title)
    t.appendChild(b)
    t.appendChild(el('span', null, text ? ' · ' + text : ''))
    wrap.appendChild(t)
    setTimeout(function () { t.remove() }, 3000)
  }

  function reply(text) {
    var hit = text.indexOf('人工') >= 0 || text.indexOf('客服') >= 0 ? HUMAN : null
    if (!hit) {
      hit = KB.filter(function (item) {
        return item.k.some(function (k) { return text.indexOf(k) >= 0 })
      })[0]
    }
    var wrap = typing()
    setTimeout(function () {
      wrap.remove()
      if (hit) {
        var msg = agentMsg(hit.a)
        if (hit.kb) msg.lastChild.appendChild(knowledge(hit.kb))
        if (hit === HUMAN) {
          handover = true
          $('agent-av').textContent = '陈'
          $('agent-name').textContent = '陈思远'
          $('agent-status').textContent = '人工客服 · 已接入'
          sysline('客服 陈思远 已接入本次会话')
          setTimeout(function () {
            agentMsg('您好，我是人工客服陈思远，已看到您和机器人的对话记录，我来继续为您处理～')
            csat()
          }, 900)
        }
      } else {
        agentMsg('收到～这个问题我需要确认一下，您也可以回复「转人工」直接接入真人客服，或补充订单号我帮您查询。')
      }
    }, 900)
  }

  function send(text) {
    var value = String(text || input.value).replace(/\s+/g, ' ').trim()
    if (!value) return
    input.value = ''
    customerMsg(value)
    reply(value)
  }

  var api = {
    open: function () {
      $('chatbox').classList.add('open')
      $('fab-badge').hidden = true
      scrollBottom()
    },
    close: function () { $('chatbox').classList.remove('open') },
    toggle: function () {
      $('chatbox').classList.contains('open') ? api.close() : api.open()
    },
  }

  $('fab').addEventListener('click', api.toggle)
  $('btn-close').addEventListener('click', api.close)
  $('btn-min').addEventListener('click', function () {
    api.close()
    $('fab-badge').hidden = false
  })
  $('btn-send').addEventListener('click', function () { send() })
  input.addEventListener('keydown', function (e) {
    if (e.key === 'Enter' && !e.shiftKey) {
      e.preventDefault()
      send()
    }
  })
  Array.prototype.slice.call(document.querySelectorAll('#quickq button')).forEach(function (btn) {
    btn.addEventListener('click', function () { send(btn.dataset.q) })
  })

  // 自动开场：让访客一眼看到组件，而不是先看到空页面
  setTimeout(function () { api.open() }, 700)
</script>
</body>
</html>
```

> ⚠️ 演示页里的对话是**拟真演示**，不会产生真实会话；要用真实渠道体验，去控制台「渠道接入 → 测试会话」。

### 6.2 步骤13：协议与隐私页

把 `legal.html` 放进官网项目，页脚与 FAQ 里指向它即可，关键点：

-   顶部导航切换「服务协议 / 隐私政策」两个文档（`?t=terms` / `?t=privacy`），SEO 上给文档页加 `noindex`，避免和官网首页抢收录；

-   内容建议用你法务认可的版本，本仓库里的那一份是示例文本，替换时保持文件名与链接不变。

## 运行与验证

### 7.1 步骤14：在 IDEA 里一键运行

1.  IDEA 打开 `yunti-official` 目录；

2.  运行下拉里选 **「官网预览」**（项目自带共享运行配置），点绿色 ▶；

3.  控制台会打印访问地址，点开就是官网首页；点红色 ■ 停止。

看不到「官网预览」时：`File → Reload All from Disk`，或直接右键 `scripts/serve.py` → Run。

### 7.2 步骤15：在终端运行

``` code-block-container
cd yunti-official
./serve.sh                    # 首选：自动挑 Python（没 Python 才退回 Node）
./serve.sh --port 5181        # 换端口
./serve.sh --no-open          # 不自动打开浏览器
```

访问地址：[http://localhost:5180](http://localhost:5180/)

  

<img src="https://article-images.zsxq.com/FogwPK74IClqmfl19BCU8MX3gY8E" class="tiptap-image" alt="图片.png" />

  

<img src="https://article-images.zsxq.com/FuLs67XVu30Nu8Po5USI2R2TlTOc" class="tiptap-image" alt="图片.png" />

  

<img src="https://article-images.zsxq.com/FhYwTzK9tXu-lCfghqhKdqP4wraT" class="tiptap-image" alt="图片.png" />

  

<img src="https://article-images.zsxq.com/FvT-Kj0g8ZtM4Mx3WPAUYvdXfQeL" class="tiptap-image" alt="图片.png" />

  

<img src="https://article-images.zsxq.com/FruOvxs7G-bvUZlCfL-rkUUZczvc" class="tiptap-image" alt="图片.png" />

  

<img src="https://article-images.zsxq.com/FlFZ4ZAT9HriSpTvheTWTVwy7-bj" class="tiptap-image" alt="图片.png" />

  

<img src="https://article-images.zsxq.com/FnG_5-LDC3tU6j2ypo0EQ900o4DX" class="tiptap-image" alt="图片.png" />

  

<img src="https://article-images.zsxq.com/Fo9kE_Idqx4gC79zGAj2_Wh2ix-Q" class="tiptap-image" alt="图片.png" />

点击右上角的登录按钮：

  

<img src="https://article-images.zsxq.com/FkQS08g6b3WZ26xC4RknoUDBsZmS" class="tiptap-image" alt="图片.png" />

点击

## 总结

恭喜你！🎉 你已经成功给客服系统增加了一套完整的对外官网！

**你学会了：**

-   ✅ 用「公开只读接口」把后台数据安全地喂给官网（免登录也能保证权限边界）

-   ✅ 用 `data-cfg` 契约把「页面文案」和「后台配置」解耦，改文案不用发版

-   ✅ 静态兜底 + 接口降级的组合拳：后端异常也不影响官网可用性

-   ✅ 零依赖静态站点的本地预览、构建与 nginx / Docker 部署

-   ✅ 把「官网该有什么」写成可执行的自检，防止后续改动把功能改坏
